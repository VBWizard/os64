# Public-key authentication uses authorized_keys on the config search path.
# Host identity is persistent state at /home/sshd_host_key.
port = 22
# loopback: `ssh -L` may reach services on 127.0.0.1 (REMOTE.md). none: refused.
forward = loopback
