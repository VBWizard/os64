# os64 Technical Debt Ledger

*The single sortable index of every known debt, harvested from the design
docs. Each subsystem doc remains the SOURCE OF TRUTH for its own debts (full
rationale, code sites, gotchas live there); this file is the cross-cutting
worklist you scan on a fix day to answer "what's the highest-value cheap fix
right now?" — a question no single doc can answer.*

**Maintenance rule:** when a debt is paid, delete its row here AND update the
owning section in its source doc (the two must not drift). When a review adds
a debt to a subsystem doc, add its row here too. Severity/cost are
hobby-scale judgment calls — re-rank freely.

- Severity: **Hole** (real defect exploitable/crashing) · **Robustness**
  (turns silent failures into loud ones, or prevents a known future crash) ·
  **Feature-gate** (blocks a planned feature) · **Cleanup** (honesty,
  cruft, perf-at-scale).
- Cost: **XS** (minutes) · **S** (an hour) · **M** (a session) · **L**
  (multi-session / discipline-wide).

## Quick wins (high value ÷ low cost — start here on a fix day)

| Debt | Sev | Cost | Source |
|---|---|---|---|
| **`file_io x2 (concurrent)` reported one exit of 0 once** (2026-08-28, QEMU GUI boot with three GUI apps running, 1 in ~12 suite runs that day). The fixture's only exit-0 path is `main` returning normally, which it never does on success (badge 0x0F11E60D) — so a 0 is either an exit code lost between task exit and `os64_wait`, or a burial handing back a stale status. Unread; two clean reruns followed. If it returns, the question is which of the pair's two tasks reported 0 and what its own serial line said | Flake | S | userland/tests/testrun/testrun.c run_concurrent_pair; kernel/test/elf/file_io.c |
| **`memory_test` flakes on FAIL_NEEDLE_STUCK (0xF3EE0009)** — twice in seven QEMU `testrun`s, 2026-08-27. Step 5 takes a memory snapshot, touches 256KB, takes another, and demands `free` fell and `used` rose. Nothing else is held still: the previous fixture's burial (kworker) can hand back more than 256KB between the two snapshots, and the needle moves the wrong way for a reason that has nothing to do with the syscall under test. Cure: compare against a DELTA the test owns — allocate, snapshot, touch, snapshot, and require `used` grew by at least the probe size MINUS whatever `free` gained, or simply retry the step a few times; a suite that cries wolf gets ignored | Flake | XS | kernel/test/elf/memory_test.c step 5 |
| **`task_teardown_leak` cries wolf when somebody is USING the machine** (2026-09-02). **Chris's diagnosis, which is the authoritative one: the test was written to run in POST-BOOT, where one core is executing and nothing else is going on.** It measures the machine-wide free-page count across a spawn→exit→burial cycle and demands the delta be zero — an assertion that is only meaningful on a quiescent machine — and it moved to the LATE phase on 2026-08-29, which is precisely the phase that runs beside a live userland. The move bought an 18-second boot; the bill is a test whose premise no longer holds where it runs. Seen while driving os64get by hand at the husk prompt: attempt 1 reported 53200 bytes lost (26600/task), attempt 2 reported 1461144 (730572/task), and it failed on its own rule that "the loss REPEATED across 2 independently measured windows, so it is not the machine". The 27× spread is the tell — a real leak in `task_create`/`task_destroy` is a fixed cost per task, not a number that swings that far between two windows eight seconds apart. **The control settles it: the same build booted with nothing typed into it measured `free 8391047560 -> 8391047560` twice, byte-identical, and passed.** Two windows a few seconds apart inside ONE burst of human activity are not independent of each other, so repeating the measurement cannot separate a leak from a bystander. Cures, in order of honesty: measure a DELTA the test owns rather than the machine's total (the `memory_test` row wants the same thing for the same reason — a position-free pointer on purpose: rows move); widen TL_DIRTY so a window that saw any other task spawn is discarded rather than counted; or move it back to post-boot and pay the seconds. Until then, read a late-phase failure of this test as a question, not a verdict — and reproduce it on an idle boot before believing it | Flake | S | soon (Chris, 2026-09-02); or the next touch to the late phase | `kernel/test/test_main.c` `teardown_leak_attempt` / DEBTS § memory_test |
| Zombie tasks hold their ELF's FAT lock slot until phase-2 burial (window = spawn rate × kworker cadence; FF_FS_LOCK=64 is headroom, not a cure) — close `image->file` at ZOMBIE transition instead, no fault can need it after the last thread dies | Robustness | S | ffconf.h FF_FS_LOCK comment, 2026-08-07 |
| AHCI driver is single-controller: `kPCISATADevice`/`ahciABAR` are globals the second controller's init overwrites — on q35 (built-in ICH9 + any added AHCI device) I/O wedges inside the FatFs volume lock (proven by RIP forensics 2026-08-08). One controller works; per-controller state is the fix | Correctness | M | ahci.c init_AHCI_device |
| AHCI driver cannot WRITE (read op only) — a SATA disk root runs read-only via the 2026-08-08 write-less-device demotion until the write half exists. (The P5 turned out to have NO SATA controller at all — its "internal" bay is USB-bridged — so this debt currently has no bare-metal customer; the P5's disk-root path is an M.2 NVMe transplant or, someday, USB mass storage) | Feature | M | GNUmakefile DATA_DISK_IF note; vfs.c demotion comment |
| `mp_schedStack` mapped USER (`0x7`→`0x3`) — ring 3 can scribble kernel scheduler stacks | Hole | XS | ABI § Memory-protection #1 |
| Enable **SMEP** (CR4, CPUID-gated) — blocks ring-0 exec of user pages | Robustness | S | ABI § Memory-protection #2 |
| ~~Allocator OOM: silent `cli; hlt` → panic naming size~~ **PAID 2026-08-25** (Fable's review of PR #29 rd15): both halts were under the allocator's own lock, so exhaustion parked one core dark and then wedged every other core at its next allocation, interrupts off, with no line anywhere. Now `panic()` by name with the size. The finding that paid it: rd15 built a fallback for `kmalloc()` returning NULL, which cannot happen in this kernel — the allocator never returns "no memory" and kmalloc adds kHHDMOffset regardless. ~44 `kmalloc(...) == NULL` guards in the tree are the same theatre; harmless, but a sweep that deletes them (or the contract line in kmalloc.h that makes them visibly redundant) is a small cleanup for a quiet day. "Largest free extent" in the message is still a nice-to-have | Robustness (PAID) | — | allocator.c `allocate_memory_at_address_internal` |
| Delete debug residue: `>= 200000000` stub in allocator + `kmalloc_dma` twin | Cleanup | XS | MEMORY #3 |
| **`/proc/self` resolves but is never LISTED — and PROC.md still says it doesn't exist.** Found by Chris 2026-08-13: `ls /proc` shows only numeric task IDs, `ls /proc/self` works fine. `proc_parse_path` handles "self" as a special component; `proc_read_dir`'s `PROC_NODE_ROOT` case walks kTaskList and emits decimal IDs only, so the name is openable and invisible. Nothing in the code says that is deliberate, and PROC.md's "What is deliberately NOT here" section STILL reads "**No `/proc/self`**… it arrives when a program demands it" — a program did, it shipped 2026-08-09, and the doc was never struck. **The doc correction is not a judgment call; the listing is.** Two real arguments: LIST IT because names in this house do not lie — a directory answering to a name it refuses to show is the same species of small dishonesty as a log level that misreports its message, and the next person will also find it by accident and wonder what else is hidden. DON'T LIST IT because /proc is processes ONLY, Plan 9 style, and Plan 9 has no `self` at all: it is an alias, not a process, so listing it stops `ls /proc \| wc -l` from being a process count. ("Linux lists it" is not an argument here.) Note the practical objection does NOT apply — `top` already skips non-numeric entries on purpose (`// any future non-task names`), so either choice is safe for the enumerators. Cost is a synthesized entry ahead of the numeric walk plus the doc fix | Cleanup (+ a ruling) | XS | procfs.c `proc_read_dir` / PROC.md § "What is deliberately NOT here" |
| `#DF` IST emergency stack — turn stack-death triple faults into panics | Robustness | S–M | ABI (deferred) |

## Security / correctness

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| `mp_schedStack` USER bit (`0x7`→`0x3`) | Hole | XS | none | ABI § Mem-prot #1 |
| SMEP enable (CPUID.(07H,0):EBX bit 7) | Robustness | S | none | ABI § Mem-prot #2 |
| SMAP enable + `stac`/`clac` bracketing (CPUID bit 20) | Robustness | L | when copy helpers are the ONLY user-access path (argv setup in task.c is the other) | ABI § Mem-prot #3 |
| Page-0 / VA-0 NULL guard — unmap the low identity window so kernel NULL derefs trap (mind the AP trampoline) | Robustness | M | none | MEMORY #5 / ABI |
| Drop IOPL=3 for ring 3 (currently ring 3 can execute `out`) | Hole | S | once fixtures stop using `out` | ABI (deferred) |

## Kernel robustness

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **UBSAN IN THE KERNEL — the compiler already knows about undefined behaviour, and os64 never asks it.** `-fsanitize=undefined` turns the things the C standard leaves undefined — signed overflow, a shift wider than the type, a misaligned pointer, a null dereference, an array index the compiler can bound — into runtime reports at the site. THE REASON IT MATTERS IS NOT TIDINESS: UB is not "behaves oddly", it is "the optimizer is ENTITLED to assume you never do this", so the failure mode is a check the compiler deletes rather than a wrong answer you can see. **This is the CHEAP half of the sanitizer family and the only one that fits a freestanding kernel today**: it needs no shadow memory and no allocator hooks — just a handful of `__ubsan_handle_*` functions, four lines each, calling `printd`. Linux ships exactly this as CONFIG_UBSAN. Turn it on for a boot, read what falls out, then decide whether it lives behind a cmdline token or a build flag. **ITS BIG SIBLING, KASAN, IS A REAL ARC AND NOT THIS ROW**: byte-granular poisoning needs shadow memory (1/8 of all RAM), allocator hooks and early-boot ordering — and os64 already runs the PAGE-granular ancestor, since the lazy HHDM unmaps freed physical memory so a use-after-free faults (CLAUDE.md names it DEBUG_PAGEALLOC-style). KASAN is that same instinct at byte resolution, which is what catches a three-byte overrun off the end of a row. **HOW THIS GOT BOOKED:** Codex ran the libimage host suite under AddressSanitizer and found a heap-buffer-overflow in our own test fixture, in a run that was printing PASS (PR #30 rd3). The host harness is the ONLY place in this tree where the sanitizers can be linked at all — a freestanding kernel has no runtime for them — which is both why that catch was possible and why the kernel has been flying without any of it | Robustness | S (UBSan) / L (KASAN) | a quiet day — it is a read-what-falls-out slice, not a fix-list | Codex #30 rd3; Chris asked what the flag actually does, 2026-08-25 |
| **Task teardown v1 SHIPPED 2026-08-06 (the graveyard shift) — residuals only.** `task_destroy()` exists: kworker buries COLLECTED corpses (retValCollected via wait/reap, autoReap by decree, or orphans) in a two-phase sweep (unlink pass N, free pass N+1 — grace period for lockless /proc walkers), freeing per-thread stacks (the 1MB+64KB per command), thread_t + TID + syscall scratch, path/cwd, static elf_image_t + its open backing file (un-haunts ext2's rm-refusal), and the task_t. **2026-08-13: three of the four residuals PAID, and the survivor is now COUNTED rather than merely admitted.** (3) page-table pages died with the paging arena (a8c952c — `arena_destroy` at burial takes the PML4 and every PDPT/PD/PT in one motion; the pool's bump allocator no longer has anything to do with a task's tables). (2) argv AND env blobs are freed — the row's own dragon warning, "env is CoW-inherited by children", was FALSE and had always been: `env_inherit` (env.c) does a plain memcpy into a fresh block and env.h says so in writing, so every task's environment is exclusively its own. (4) mmaps/shared_objects dlist nodes + every `vma_t` are freed, and burial now RELEASES the one shared_object reference `task_create` took (`shared_object_release`) — one edge on the main image, identified by identity, NOT one per list node: `task->shared_objects` holds the whole closure while the task owns a single reference, so per-node release would drive every library's count negative on the second burial (the pairing rule is written in shared_object.h). refcount is therefore now a true count of live holders; UNLOAD-at-zero is deliberately NOT implemented — retention is its own slice, same split ruled for the block read cache. Also found and paid the same day: the ring-3 exit trampoline page (one `kmalloc_aligned` page per user task whose kernel pointer was a LOCAL that went out of scope) — 4KB per command, on no ledger and in no notes doc. **2026-08-15: the last residual is PAID — burial reclaims the VMA backing pages.** The deferral's bill arrived on the P5: each buried task's booked pages pinned `in_use` rows in the allocator's status table forever (dead-owner rows can never merge or compact), `watch -n 1 "ps -ef"` minted ~18 rows/sec, every allocator op is O(rows) (the machine visibly slowed), and at ~5,500s the then-unchecked table walked off its own end onto the kernel PML4 — the logd #PF that birthed the watchpoint subsystem. The ownership question the deferral waited on was answerable without refcounts TODAY: the shared_object page cache is os64's only page-sharing mechanism and `so->page_phys[]` is its authoritative registry, so the burial walk frees every resident frame the registry disclaims (`task_frame_is_shared_object_cache` — written 8/13 as ledger honesty, promoted 8/15 to reclaim guard) and retains the cache's. The page-refcount ruling stays booked to the fork/CoW arc — task-to-task sharing of anonymous pages is the day a registry stops being enough. THE PROOF grew teeth: `task_teardown_leak` (in the LATE phase since 2026-08-29) spawns `/tests/glutton` (a deliberately fat fixture — multiple text pages, dirtied .data, 4 bss pages, 4 heap pages it never unmaps) and asserts a spawn→exit→burial cycle's allocator delta is ZERO **and** that `kTaskVmaReclaimedBytes` climbed by at least the fixture's data+bss+heap floor (so a demand-paging break can't pass vacuously). Also still true: `task_create` must validate before building (`elf_can_load`) — task_destroy only buries the fully-born; a mid-construction unwind path is separate work. And the /proc grace-period discipline deserves a grown-up successor (snapshot-under-lock or generation counts) | Residuals paid; row survives for the two "also still true" items | S | mid-construction unwind: when task_create grows a failure path worth having | `task.c` undertaker / notes doc |
| **FIXED 2026-08-13 — the graveyard race: an unlocked list append leaked whole tasks.** `task_enqueue_dead_child` was a read-modify-write on shared state with NO lock: read `parent->deadChildTail`, write `tail->deadChildNext`, store the new tail. Two siblings exiting on two cores at the same instant both read the same tail, the second's `->deadChildNext` write silently discarded the first's, and the last store won — leaving ONE child on no list at all. Not a zombie (a zombie is reachable and deliberate) but INVISIBLE: kworker's sweep only ever walks these lists, so an unlisted corpse can never be examined, collected, or buried, and every byte it owns leaks for the rest of the boot. Reproduced in `test_dynamic_linking`, which spawns two tasks that exit on the same tick on different APs and lost one of them ROUGHLY HALF THE TIME — for as long as that test has existed. The symptom was pure absence: no panic, no error, one task quietly missing from a list nobody prints. Fix: `kDeadChildLock`, one global irqsave spinlock over every deadChild list (global not per-task because the orphanage touches two parents' lists in one motion, and a per-task ordering is a deadlock waiting for its first mistake). Lock order is kDeadChildLock → scheduler queue lock, safe BY CONSTRUCTION since nothing in scheduler.c touches a deadChild list; the two calls that would have inverted it — the parent wake, and `*exitCode`'s store to a caller pointer that can fault — are deliberately performed after the release. Verified 9/9 boots clean against a ~50% pre-fix failure rate. Left behind as a tripwire: the sweep now REPORTS a collected-but-unburiable corpse on DEBUG_TASK instead of silently stepping over it every 2s forever | Hole (FIXED) | — | done | `task.c` `task_enqueue_dead_child` / kDeadChildLock |
| **PAID 2026-08-22, on the day the condition it was waiting for came true.** The row read "when userland gains dynamic binaries" and userland gained them that afternoon, so the panic had to go the same day. `elf_resolve_dynamic_dependencies` now returns false and `task_create` unwinds; `shared_object_load_or_get` returns NULL for every failure it can have (missing file, malformed ELF, wrong e_type, unloadable `DT_NEEDED`, exhausted virtual window, out of memory), unwinding a partially-registered object — deps released, registry node removed, window slot reclaimed if it was the last bump. The FAULT path was paid too, and it was the bigger half: `shared_object_resolve_page` had four panics of its own (page alloc, read failure, relocation failure) that a corrupt `.so` could reach from ring 3 at any moment long after load; each now releases its `page_phys[]` slot back to 0 — never leaving it RESOLVING, or every core spinning on that page would spin forever — and returns 0, which the fault handler turns into a segfault-kill of the faulting task rather than a machine panic. Proven live: a binary whose `DT_NEEDED` names a library that does not exist answers `husk: cannot run nolib` and the shell keeps its prompt | Hole (FIXED) | — | done | `task.c` / `shared_object.c` / `simple_exceptions.c` |
| ~~Allocator silent-OOM → panic~~ PAID 2026-08-25 — see the Quick-wins row | Robustness (PAID) | — | done | allocator.c |
| `#DF` IST emergency stack | Robustness | S–M | none | ABI (deferred) |
| MAX_CPUS=24 vs. the 3900X's 24 threads (index 24 = one past every per-CPU array) — raise the constant or guard with MAXCORES | Robustness | S | before booting the 3900X | SCHEDULER #5 |
| **Shutdown verb: kernel side PAID 2026-08-08 pm** (SYSCALL_SHUTDOWN 39 → `shutdown_system()`: logd retire handshake via klog_read's RETIRED sentinel, emergency ring flush — added after the descent's first test flight dropped a whole boot's log, its one scar — `vfs_sync_all`, NVMe FLUSH CACHE per device, hypervisor poweroff ports, 1995 liturgy + park on bare metal; SHUTDOWNTEST limine entry = the scriptable end-to-end check, QEMU exiting = pass). REMAINING: `/bin/shutdown` + husk wiring (Chris's, by his call); hard AP quiesce via IPI-halt (soft today: tickless parks idle APs, logd retired, shell blocked — quiescent in practice, not by construction); ATA FLUSH CACHE rides the AHCI-write row | Robustness | S each | /bin/shutdown: none; AP-halt: before shutdown runs on a busy multi-user-ish system | shutdown.c descent comment |
| **No native fsck / mount-time dirty check**: os64 mounts whatever the superblock claims, unverified — a torn disk mounts writable and compounds the damage. Today's auditor is host-side e2fsck from Ubuntu, which works but means the OS can't vouch for its own disks. v1 is NOT a repair tool: read-only verify (superblock sanity + the ext2 `s_state` clean flag honored at mount, warn-and-forced_ro on dirty) buys most of the safety for a fraction of the cost; a real repairing fsck.ext2 is its own arc | Robustness | v1 S, full L | after the shutdown verb (a clean-unmount flag is only meaningful once unmounting cleanly is possible) | ext2.c mount path / Chris 2026-08-08 |
| **IRQ0's EOI can go to the wrong controller, stranding a LAPIC ISR bit forever** (found by inspection 2026-08-10 while chasing the VBox freeze). `handler_irq0_asm` picks its EOI target by branching on `kIRQ0UsesLapic` — LAPIC if set, legacy PIC (`out 0x20`) if not. But `ioapic_adopt_isa_irq` (apic.c) sets that flag LAST: `ioapic_route_irq()` makes the redirection entry live, the PIC line is masked, and only THEN does `*uses_lapic_flag = true`. An IRQ0 delivered through the IOAPIC inside that window is acknowledged to the PIC while its LAPIC ISR bit stays set — and a set ISR bit is never cleared by anything else, so PPR pins at that vector's class for the rest of the boot, blocking every lower-priority interrupt permanently. Today the vector is 0x20 (class 2), so a stranded bit blocks almost nothing and the bug is invisible; that is exactly why it survived. It stops being invisible the moment anyone raises IRQ0's priority (see the wall-clock row) or lowers something important beneath it. Fix is one reorder — set the flag BEFORE the routing write, since a PIC EOI for an interrupt that never came is harmless while the reverse is fatal. NOTE the limit of the evidence: this does NOT by itself explain why 0x20 boots VBox cleanly and 0xE0 does not, so it is a real defect found on the way, not a proven cause of that freeze. Confirming a stranded bit needs one reading — LAPIC ISR (0x100..0x170) on a wedged guest | Hole (latent, lethal under any priority change) | XS | none — cheapest possible insurance against a whole symptom class | apic.c `ioapic_adopt_isa_irq` / handler_irq0_timer.S |
| **UNEXPLAINED, SEEN ONCE (2026-08-10): allocator panic on task teardown, `get_status_index_for_requested_address` (allocator.c:241) — "Can't find the index for 0x0000000100298360 (len=0x0, in_use=1)".** Fired immediately after `husk: pid 48 exited code 137` in periodic mode under load (hog on VT1, top on VT2), at tick 3980 of that boot. The address is non-page-aligned and `len=0x0`, which reads as a free-path lookup for something the allocator does not own — a double free or a freed-with-wrong-length during teardown is the shape to look for; the deferred-teardown residuals row above is the obvious neighborhood. TWO co-occurring oddities that may be one story or three: (1) hog EXITED AT ALL — with no argument its burn loop has no exit path (`endTick` stays 0), and it was NOT reproducible on the next run; (2) exit code 137 may be a REPORTING artifact rather than a real kill, since `task_terminate_sibling_threads` stamps SIGKILL on siblings of any exiting task and `syscall.c:699` then derives the task retVal from a SIGKILL-marked thread — so a normally-exiting multithreaded task can report 137. Full evidence (serial capture with all three panic copies, tick timeline) was in this session; the panic DID reach the wire, so a repro is greppable. Not attributed to the same day's scheduler/IRQ0/settle changes — but not cleared of them either, because it has not recurred | Hole | M | none — first recurrence should be chased with the frozen-guest technique while it is still up | allocator.c:241 / task.c task_terminate_sibling_threads |
| **A disabled `printd` still evaluates its arguments — so a log line nobody prints can kill the machine.** `printd` is a real varargs FUNCTION (printd.h), not a macro, so the debug-level test happens INSIDE the callee: every call site pays the argument evaluation and the call regardless of level, and any argument that dereferences a pointer dereferences it even when the level is off. Known and accepted from the beginning (the cost was the point — one uniform function, no macro soup), booked 2026-08-10 when the scheduler re-entrancy hunt found the crash site at scheduler.c:1286 was a `printd` whose args walk `cls->currentThread->ownerTask` — a *silent* log line reaching through a corrupted pointer and taking the #GP that a corrupted pointer deserves, but at a line that looks inert. Cure: gate evaluation at the call site — `#define printd(lvl, ...) do { if (DEBUG_ENABLED(lvl)) printd_impl(lvl, __VA_ARGS__); } while (0)` — which also stops paying for logging the OS isn't doing. The reason it's a slice and not a one-liner: evaluation stops being guaranteed, so every call site must first be swept for arguments with side effects | Robustness | M | none — every disabled `printd` with a pointer deref in its args is a live tripwire pointed the wrong way | printd.h / serial_logging.h; scheduler.c:1281,1286 |
| `free_memory` return value must never be used as an index post-merge/compaction (documented at both ends; latent footgun) | Cleanup | XS | none | MEMORY #4 |
| `mp_timesEnteringScheduler` stride mismatch: `uint32_t[]` in C, but scheduler.S increments `qword ptr [.. + rax*8]` — core N's asm bumps elements 2N/2N+1 while C reads element N (first-pass check reads a value asm never touched; masked by `mp_CoreHasRunScheduledThread`). Make it `uint64_t[]` in C | Hole (latent) | XS | none — found 2026-07-10 during tick diagnostics | SCHEDULER (scheduler.S / scheduler.c) |
| **A panic's serial write can silently not happen** (2026-08-08, ring-3 shutdown's no-VMA panic): the banner+message reached the GLASS but never the wire — qemu_com1.log shows the OTHER cores' logd heartbeats ticking on around the moment of death, no panic text. That is the 7/18 doctrine violated in a new scenario (fault taken inside call_in_kernel_context's continuation, panic from the #PF handler); the specific panic is extinct (fixed same day) but the pipeline gap is not diagnosed. Related and visible in the same log: panic halts ONLY the panicking core — the APs keep running (and logging) through a panic, which both muddies forensics and is its own wrongness. Hunt = reproduce via a deliberate fault in a cikc continuation, then walk panic_broadcast's serial path; the AP-halt half likely wants the same IPI-halt vector the shutdown descent's hard-quiesce follow-on wants — one vector, two customers | Hole | M | none — every panic in this scenario pays until diagnosed | panic.c / simple_exceptions.c / Chris's 2026-08-08 shutdown log |
| **Panics print raw stack RIPs, no symbolization** — os32 had real traces (`stack_trace.c`: EBP frame-chain walk + in-kernel `.symtab` scan → `module:function`, cascading process→libraries→parent→kernel); os64 regressed to a decoder-ring workflow (addr2line on the host). Design sketch, ratified in spirit 2026-07-26: (1) get the kernel's own ELF via **Limine's kernel-file request** and parse symbols **once at boot into a sorted RAM table** — the panic path may touch NO filesystem and NO kmalloc (the `/partition_info` corpse: that panic fired *because* the fs was unreadable; a trace that reads the disk deadlocks on its own cause); (2) walk the **RBP chain** (honest at `-O0`; the day we build `-O2`, `-fno-omit-frame-pointer` goes in or the chain lies), hopping the 5-QWORD interrupt frames so the trace crosses from fault back into the syscall; (3) `.symtab` name+offset is v1 — Chris leans toward **full debug info (DWARF file:line) loaded at boot** as the destination; the DWARF line program is a real interpreter, so it's a follow-on slice, not a blocker | Robustness | M | none — every future panic pays interest until this lands | panic.c / os32 `stack_trace.c` precedent |

## Userland-gating (blocks the shell roadmap)

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **Interruptible syscall bodies** — `sti` after entry frame / `cli` before sysret; `g_saved_cr3` per-CPU→per-thread; audit handlers | Feature-gate | M | HARD GATE before `read` (userland step 2) | ABI § Interruptibility |
| RAX:RDX error convention (resolved in design) | Feature-gate | S–M | with libos64 scaffolding | ABI § register contract |
| Natural typed handler signatures via SYSCALL_DEFINE cast (resolved in design) | Cleanup | S | with the RAX:RDX refresh | ABI § add-a-syscall |
| Fork register-load path (`scheduler_load_thread` panics on `justForked`) | Feature-gate | M | when fork/exec is implemented (both patterns now first-class; fork is the author's favorite — treat it well) | SCHEDULER #1 |
| libos64 `LIBOS64_HIDDEN` macro + `-fvisibility=hidden` build wiring (PLT-free internal calls, the modern replacement for libChrisOS's `I` twins) | Feature-gate | S | first libos64 scaffolding task | LIBOS64 § shared object |
| libos64 buffered `<os64/stdio.h>` layer (FILE*/fread/fwrite over raw handles) | Feature-gate | M | second phase — raw `<os64/io.h>` is enough for the shell | LIBOS64 § handles |
| **PAID 2026-08-15 — the heap.** `map`/`unmap` landed 2026-07-19 and the libos64 `malloc` on top of them today (`libos64/heap.c`): boundary tags with Lea's in-use bit, one first-fit free list, pools for small + a dedicated region per allocation ≥128KB, a virgin frontier that makes `calloc` free on first touch, address-tied canaries, 0xA5 poison on free, and heap crimes that KILL (0xF12EEBAD / 0xCA9A12ED — Chris's ruling). An emptied region goes home to the kernel from the middle of the heap, which brk structurally cannot do. Proven three ways: `tools/test_heap_host.c` (124 host checks incl. a 20,000-round soak and the shrink-coalesce regression), `/tests/malloctest` on the roster (three entries — two of which PASS BY DYING), and `/proc/<pid>/heap`, whose audit identity (mapped == live+free+overhead+virgin) the kernel checks on every read. RESIDUE, booked below: the general userland mutex, the big-allocation address-space churn, and the fact that no APP allocates yet | Feature-gate | — | PAID | MALLOC.md / `libos64/heap.c` |
| ~~**Userland signal delivery.**~~ **PAID 2026-08-23/24 — PR #29, the signals arc (SIGNALS.md is the record; 21 Codex rounds).** Registration (syscall 49), three delivery paths (syscall exit, scheduler visit to a spinner, page-fault SIGSEGV), `sigreturn` (50), `OS64_INTERRUPTED` from every blocking call, SIGPIPE/SIGHUP/SIGTERM/SIGINT/SIGSEGV all catchable. The fifth customer, SIGWINCH, was paid 2026-08-25 (the resized-gterm row below; PTY.md § Resize). Original text kept below as the customer list. — `SIGPIPE`'s default action (terminate) was enforced *inside the kernel* (`raise_sigpipe_and_die`) because ring 3 cannot install a handler yet. Consequence: a program that wants to SURVIVE a vanishing reader can't — it always dies. Fix = deliver signals to ring 3 (handler + a way to return), after which `pipe_write` can hand `PIPE_ERR_CLOSED` back to a program that asked for it. **TWO MORE CUSTOMERS ARRIVED 2026-08-21**: SIGHUP and SIGTERM both default to death and neither can be caught, so a GUI app cannot say "wait — unsaved work" when its terminal hangs up or the machine goes down. Chris asked how to subscribe to SIGTERM the day it was built; the honest answer was "you can't yet, and this row is why". The shape when it lands: register a handler + a delivery trampoline (kernel pushes a frame on the user stack, redirects RIP, the handler returns through a stub that resumes the interrupted context) — os64's answer to sigaction, without POSIX's restart/mask warts. **DESIGNED 2026-08-23 — the full record is SIGNALS.md**, written before the building the way MALLOC.md and SIGINT.md were: identity becomes a NUMBER (killing signals.h's bit-value-indexing landmine), the handler moves to the TASK while the pending set stays per-thread (because the aim is already a broadcast, per-thread handlers would run one SIGTERM once per thread), the return stub is a second template in the existing `TASK_EXIT_TRAMPOLINE_VIRT` page (the stack trampoline every historic Unix used is outlawed here by NX), and an interrupted call returns INTERRUPTED rather than growing SA_RESTART and EINTR. A FIFTH customer arrived the same day: SIGWINCH has nowhere to be delivered, which is why a maximized gterm still reports 38x100 | Feature-gate | M | when a program needs to catch SIGPIPE, SIGHUP or SIGTERM | **SIGNALS.md** / `signals.h` / `syscall.c` |
| **Checkpoints conflate "will ever be caught" with "deliverable right now."** A masked-with-handler signal correctly counts as caught (so a second Ctrl+C during a handler no longer executes the program — the 2026-08-24 review fix), but the park loops and blocking-call bails use the SAME predicate, so a handler that itself BLOCKS while its own signal is pending again gets `INTERRUPTED` from every blocking call until it returns, in a hot loop. Accepted wart, named at birth (SIGNALS.md §7). **NARROWED 2026-08-25 (Codex #29 rd17), and the narrowing is the part worth knowing**: the reprieve now covers only the handler's OWN signal. `signal_has_handler_for_pending` walks every pending bit and answers "no reprieve" the moment it finds one whose default is death and which has no handler — so a stuck handler can no longer shield an unhandled SIGTERM (it could, indefinitely, until that round). What survives is exactly the hot-loop wart: a handler that blocks while ITS OWN signal is pending again. The cure is unchanged — two predicates: kill sites ask "will anything EVER catch this?", park/bail sites ask "can it deliver NOW?" — a held signal then parks normally and delivers after `sigreturn` unmasks | Cleanup | S | if a real handler ever blocks (none does today — sigdemo's is two stores) | SIGNALS.md §7 / `signal_has_handler_for_pending` |
| **Named pipes (FIFOs).** Same pipe object, same semantics — the ONLY difference is the rendezvous: a VFS name instead of inheritance across spawn. A FIFO holds no data (0 bytes on disk forever); it is just a name that resolves to a kernel pipe object, and `open()` blocks until both a reader and a writer arrive. `pipe.c` was written unaware of how it was created, precisely so this is additive. Also the first VFS node that isn't a file — the same door **procfs** walks through | Feature-gate | M | when two UNRELATED processes need to talk | `pipe.c` / VFS |

## Virtual terminals (the VT slice, 2026-08-08 — booked at birth)

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **Widget plane has no backing store**: `print_at` paints raw glass, so a VT switch's repaint blows widgets away until their owner reprints (the clock self-heals in ≤1s, which is why v1 ships this way). The doctrine says widgets SURVIVE an F3; honest survival needs either a widget cell layer the repaint composes over, or a repaint-notification the owners subscribe to | Cosmetic | S–M | when a widget that repaints slower than the clock exists | syscall_numbers.h § widget doctrine / tty.c repaint |
| **No serial TTY backend**: serial is output-only (printd/logd) — no read path, no IRQ4, no tty_t bound to a UART. The tty_t object was shaped so a serial console is "just another sink + source"; building it is the day os64 gets a headless login | Feature | M | when the P5 (or the 3900X) wants headless use | tty.h layering note / serial.c |
| **No job control**: Ctrl+Z/SIGTSTP/`fg`/`bg` don't exist; per-tty fgTask made them POSSIBLE (the hard prerequisite is paid), but stop/continue semantics, husk verbs, and SIGCONT delivery are their own slice. **A SECOND CUSTOMER ARRIVED 2026-08-22**: the debugger. `/proc/<id>/mem` shipped read-only; its Plan 9 siblings `regs` (the saved frame — meaningful only while the task is STOPPED) and `ctl stop`/`start` are the next slice, and `stop`/`start` IS SIGSTOP/SIGCONT under its better name. Chris is sold; build them together | Feature | M | the debugger (regs + ctl stop/start), and Chris wanting to park an editor | signals.h SIGSTOP/SIGCONT stubs / procfs.c mem |
| **`/proc/<id>/mem` v1 reads only PRESENT pages** — a page the task owns (in a VMA) but has never touched has no PTE yet, so `maps` says it exists and `mem` says `??`. v2 populates on the reader's behalf (fault the page in through the task's VMA, then copy) — Chris: "may happen quickly depending on whether ?? is annoying". Also: the write path, gated on a debugger needing an int3, must document that an HHDM write ignores the task's PTE protections | Feature | S | the first annoyed `??`, or the first breakpoint | procfs.c proc_mem_read |
| ~~**GUI console window is not a tty sink**~~ **PAID 2026-08-19 by the VT8 chapter — and not the way this row imagined.** The row proposed teaching `kConsoleSink` about ttys: bind tty N's grid as a window surface so VT switching and window focus became one idea. What shipped deleted the sink outright — glass ownership IS VT focus, the compositor is VT8's seated shell, and the console window retired alongside the diversion (ruled: no ring-0-rendered windows at all). The unification the row wanted did happen; it arrived as "the GUI is a terminal like any other" rather than "a terminal is a window like any other", and the shell-in-a-window half became Phase E's `gterm`, in ring 3 where it always belonged. Row struck during the resize slice, having gone false the night before | Feature | — | PAID | GRAPHICS.md VT8 chapter / tty.c + BasicRenderer.c headstones |
| **The text-console mouse needs a GUI boot**: `vt_select.c` gives a text VT selection, copy and paste — but only when `GUI` is on the cmdline, and Chris met this the day it shipped (2026-08-21, and called it "not an issue"). TWO reasons, both in the plumbing: `mouse_init()` + IRQ12 adoption sit inside `if (kEnableGUI)` in kernel.c, so a text-mode boot never brings the AUX port up at all; and the input ring's only drainer is the compositor thread, which does not exist without the GUI. The fix is correspondingly two-part: bring the mouse up unconditionally (or behind its own flag), and give the ring a second drainer — `tty_flush_if_dirty`'s ~30Hz rider is the natural home, since it already runs on every boot for exactly this cadence | Feature | S–M | when someone wants a mouse on a GUI-less boot | kernel.c § input IRQ adoption / vt_select.h |
| **No ETXTBSY**: nothing stops a program opening a RUNNING executable for write and truncating it in place. os64 demand-pages text from the inode, so the running process would fault in a mix of old and new bytes and die confused — Unix added ETXTBSY in V7 for precisely this. Not currently reachable in practice: `os64get` publishes through `<dest>.part` + verify + atomic rename (a NEW inode; the running copy keeps the one it holds, via the ext2 orphan chain), which is the idiom every installer here uses. The debt is that the SAFETY lives in the tool's manners rather than in the kernel's refusal | Robustness | S–M | when a second tool learns to write into /bin | os64get.c § publishing / elf_loader.c |
| **BUILT 2026-08-22 — the gate ("when a refresh is more than three files and he is tired of typing them") opened the same day shared libraries made the payload 66 files.** `LIST\n` → `<name> <length> <crc32hex>\n` per file → `.\n` (SMTP's 1982 terminator, still typeable by hand at 1am). The size and crc are not used to decide anything yet — they cost the server nothing, let the client print an honest "N files, M bytes" before committing, and are exactly what a future skip-what-I-already-have would need (deliberately NOT built: the whole payload is under 4MB at 392KB/s). `os64get -a HOST` fetches every name and lets `/etc/os64get.conf` place each one, sharing ONE dated archive directory for the whole run — which is what makes the archive read as "the system as of then" rather than 66 separate installs. Every file is attempted even after a failure (a refresh that stops halfway leaves a system that is neither old nor new), failures are named twice, and a run that lost even one file exits non-zero. **TWO-PHASE COMMIT (Chris's call the same evening, after his first real 86-file refresh): "if this fails halfway through, it could get ugly."** Right, and the reason is that installing as you go makes the window in which the machine is half-old and half-new the WHOLE TRANSFER, with a torn system as the outcome of any interruption. So `-a` now fetches everything first — each file verified into `<dest>.part`, nothing published — and only then commits, one rename per file inside one directory, no data moving. If ANY file fails to arrive, nothing is installed at all and every staged file and archive entry is swept away. The wire target was flipped to make that possible: it lands beside its DESTINATION now and the archive is the verified copy of it, because the old order (archive as master) put a cross-partition copy inside the commit step. Not atomicity — 86 renames can still be interrupted at rename 40 — but the window goes from tens of seconds of network to milliseconds of metadata, and every detectable failure is detected before anything is published. **Verified live both ways: 83 files installed in one run into one archive folder; and with the valet killed mid-run, "42 of 83 files did not arrive — INSTALLING NOTHING. The system is untouched", with no staged file left behind.** A true all-or-nothing commit would need a staging directory swapped in one motion, which os64 has no verb for — booked below | Feature (DONE) | — | done | os64get.c / tools/os64serve.py |
| **A multi-file refresh's COMMIT phase is not atomic** — it is N renames, and an interruption at rename 40 of 86 leaves a mixed system, the very thing the two-phase staging exists to avoid. Staging shrinks the exposed window from the whole transfer (tens of seconds of network) to the commit (milliseconds of metadata, no data movement, no network), which is the 99% fix; closing the last 1% needs an operation os64 does not have — publish a whole directory in one motion (a rename of a staging dir over the real one, which then needs /bin to BE a swappable directory, or a per-file journal replayed at boot). Worth doing only if a real interruption ever lands in that window, which is now improbably narrow | Cleanup | M | if a torn commit is ever actually observed | os64get.c `stage_commit` loop |
| **os64get's HTTP half has no unchanged check.** The valet's `local_matches` declines a transfer before it starts by comparing the file on disk against the length and CRC the header names; a URL fetch re-downloads unconditionally, because HTTP offers no checksum and the parser keeps no `Last-Modified` or `ETag`. HTTP's own answer is conditional requests — `If-Modified-Since` / `If-None-Match` and a 304 — which is the same idea the valet's check already implements, so the shape is known and the asymmetry is the debt. Wants a consumer first: a person re-fetching one page pays nothing, a script polling one does. Booked 2026-09-02 with the URL slice | Feature | S | when anything fetches the same URL twice on purpose | os64get.c `fetch_url` / http.c `http_response_t` |
| **A length-less HTTP response can fill the disk.** With no `Content-Length` the close is the length (RFC 1945's framing), so os64get reads until the peer hangs up with no cap on how much lands in `<dest>.part`. The backstop today is the write failing when the filesystem is full, which leaves a fragment and the previous file untouched — correct, but late. A `--max-size` (curl's shape) or a default ceiling would refuse earlier and say why. Related but NOT the same as the response expansion cap increment (d) needs for gzip, where the ratio, not the byte count, is the hazard. Booked 2026-09-02 with the URL slice | Robustness | XS | when a length-less fetch actually costs a disk, or when (d) brings its own cap | os64get.c `fetch_url` body loop |
| **os64get's URL parser refuses more of the grammar than a browser does**, each on purpose and each cheap to relax if a real address ever needs it: an IPv6 literal in brackets (the stack is v4), `user:pass@host` (os64 has no HTTP auth and a URL is a poor place to keep a password), and a space or control byte in the path (percent-encoding what a person typed is a guess about their intent, and a raw space in a request line is the request-splitting shape). Every one is refused BY NAME, so the diagnostic already says which. Booked 2026-09-02 so the refusals are on the record as decisions rather than omissions | Feature | XS | when a real URL is turned away | apps/os64get/http.c `http_url_parse` |
| **`/tmp` is never emptied.** V7's `/etc/rc` did it in one line and so will husk-as-init; until then a stray `.part` or a scratch file lives until the next root refresh. Booked 2026-08-22 when the archive design put os64get's `.part` files BESIDE their targets instead of in /tmp (rename is intra-filesystem), which is also why nothing of os64get's lands there today | Cleanup | XS | husk-as-init, or the day anything writes /tmp | etc/husk.rc / DEBTS § husk-as-init |
| **logd still carries its own copy of the `key = value` reader.** `os64_conf_read` (libos64/conf.c) was lifted from `logd_conf_value` on 2026-08-22 when os64get became the dialect's second consumer; logd was left on its private copy to keep that slice honest in size. Move it over (the callback shape was designed so logd's complaint channel survives: `key == NULL` hands it the bad line) and delete the duplicate — two parsers of one dialect is how the dialect forks | Cleanup | XS | next logd touch | logd.c logd_conf_value / os64/conf.h |
| **Pre-tty boot lines are pixels-only archaeology**: the ~6 lines printed before tty_init (memory map, paging, allocator, kernel stack) exist on glass but not in VT1's grid — the first switch away and back loses them. Deliberate (tty_init runs at the earliest possible moment, right after the shadow attaches; the lines are also all in the serial log). Recorded so nobody hunts a "missing lines" bug | Cosmetic (accepted) | — | none (fix would be scraping the shadow's pixels back into cells, which is absurd) | tty_init takeover comment |

## Performance / fairness / cleanup

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **`wait()` SPINS, and ~39 callers read as if it sleeps.** `waitTicks` burns a core on `pause` until the global tick arrives — right before there is a scheduler, and free for years because everything using it ran when nothing else wanted the core. It stopped being free the day a caller ran beside userland: the LATE test phase held a core at 100% for its whole run (Chris, 2026-08-29, watching `top`). `nap()` (time.c) is the sleeping twin — same tick cadence, parks via SIGSLEEP, falls back to the spin where no scheduler exists — and the LATE-phase loops plus `tcp_conn_dial`'s handshake poll now use it. THE REST HAVE NOT BEEN AUDITED, and a sweep is exactly the wrong shape: `nap()` is unsafe while holding a spinlock and unsafe in interrupt context, so each call site needs reading, not replacing. Worth doing where a caller can wait a long time on a live machine (the device-init paths are the interesting ones — most run pre-scheduler and are already correct by the fallback). The tell that this is worth someone's afternoon: tcp's poll loop had a comment saying "short naps" above a call that spun, so the belief is already in the tree | Performance | M | when a spinning waiter is caught costing a core on a machine somebody is using | `time.c` wait/nap; `grep -n '\bwait('` across kernel/src |
| **PAID 2026-08-06 — the buffer cache (block_cache.c).** The old row: one NVMe command per fs block, 30MB ≈ 10s. The cache's 64KB line fills ARE the coalescing (one command per 64KB cold, zero warm — 46MB wc: warm pass ≈ 2s, memcpy-bound), and both filesystems plus the partition scanner ride through the interposed bops with zero call-site changes. RESIDUE, kept honest: `ext2_bmap` still kmallocs scratch and re-asks for indirect blocks per data block — the cache absorbs the DISK cost of that (indirect lines stay hot) but the CPU churn remains; a per-handle indirect-block cache in ext2_handle_t retires it if it ever shows in a profile | Perf | S (residue) | if ext2 CPU time ever matters | the grep autopsy → the caching arc, 8/6 |
| ~~**`kmalloc_dma` guarantees nothing about WHERE.**~~ **THE IDENTITY TOOTH IS PAID, 2026-08-19** — kmalloc_dma now returns the HHDM pointer and hands the physical address back separately (the fix this row prescribed, and the doctrine e1000 refused kmalloc_dma over — its ring code is now the acknowledged original of the style). The SAME conversion retires the row's third tooth, grown on the rtl8125 branch 2026-08-16 (the kworker burial-close #PF: the identity mapping lived at a lower-half VA in kKernelPML4 ONLY, so a DMA buffer was dereferenceable from exactly one address space, and a file close doing disk I/O from a task's CR3 faulted on `dmaWriteBuffer`) — the HHDM alias is valid under EVERY CR3, so that class is structurally gone; the convention it enforced ("every file-I/O path runs in kernel context") outlives it as doctrine, in CLAUDE.md's ext2 section beside the orphan-chain note. What the conversion also killed, found during the payment: identity mappings were NEVER UNMAPPED on free, so every per-I/O NVMe PRP list left a stale writable PCD mapping over a recycled physical page — a standing hole through the lazy-HHDM use-after-free tripwire, ~1,100 new holes/sec under a logd-on-ext2 soak, plus the paging-pool churn that found the intermediate-table CAS race. All callers converted (NVMe queues/identify/bounce-buffers/PRP lists, AHCI read buffer); the PCD cache-disable was deliberately not recreated (x86 DMA is coherent; e1000 proved cacheable rings on QEMU/VBox/P5 since 8/06); `kmalloc_dma32_address` (zero callers) buried in the same grave. **THE REACHABILITY TOOTH SURVIVES, narrower**: there is still no address ceiling and no device-reachability contract — today's devices are 64-bit-DMA capable, and the first 32-bit-DMA device needs a designed bounded-phys allocator, not the buried fossil | Hole | S | before any 32-bit-DMA device | Chris's allocator experiment 2026-08-05; the HHDM conversion 8/19 |
| **PCI config space trusts an MCFG base that may be ZERO.** `readPCIRegister`/`writePCIRegister` compute `kPCIBaseAddress + PCI_MMIO_OFFSET(...)` and dereference with no guard — ECAM-only, no legacy fallback. On a machine with no MCFG table (QEMU's default i440FX board; real pre-2005 hardware) the base stays 0 and the FIRST runtime config read walks VA 0x1000 (bus 0/dev 0/func 1 against base zero) — and the failure is not a message but a TRIPLE FAULT, because the #PF's dispatcher then dies on its own GS:0 CLS read (full autopsy in VERIFICATION.md's headless section, 2026-08-18: the i440FX detour). Fix shape: refuse LOUDLY at PCI init when MCFG is absent/zero — os64 targets q35/modern hardware on purpose, so a named panic beats a port-0xCF8 fallback nobody would test. Two companion findings ride this row: (1) the page-0 unmap (long-booked) is what turns the GS:0 garbage read into a clean named #PF — promoted from someday-pile by this autopsy; (2) OPEN QUESTION: the faulting core's GS base was 0 at CPL0 inside `readPCIRegister` after 15 clean timer ticks — if any kernel path runs with user GS live, every exception on it is a triple fault, and that wants an owner | Robustness | S | the loud refusal: cheap, next PCI pass; the GS question: needs Chris's eyes | the i440FX detour, 8/18 |
| **Growable log rings.** The per-core rings are a fixed 16MB each, allocated once at `logging_queueing_init`. Designed and then DELIBERATELY NOT BUILT (2026-08-18): growth was conceived when a full ring was fatal, and it no longer is — the ring-full path now overwrites oldest and counts it, and a serial-less machine retains instead of shredding, so the two hazards growth was meant to prevent are both closed. Evidence against building it yet: 3% of one ring after a boot with DEBUG_SCHEDULER|DETAILED, `lost` = 0. **The trigger is `/sys/log`** — build it when `used` runs high or `lost` goes non-zero under a real workload. WHEN it is built: grow in the CONSUMER, never in `log_store_entry` (which runs in interrupt context and inside fault handlers, and must never allocate); cap it from the cmdline; and RE-LINEARIZE rather than realloc — a ring's `head`/`tail` are indices whose meaning changes with `capacity`, so a byte-faithful `krealloc` would hand back a buffer that looks intact and is scrambled. Giving memory back later is a separate, trickier slice (shrink after sustained low water) | Perf | M | when /sys/log says so | the logging arc, 8/18; Chris's growth ask |
| **A `tail -f` that holds ONE handle.** `ext2_handle_t` snapshots the inode AND `size` at open, so a reader holding a handle can never see the file grow — `read()` stops at the size it captured and returns 0 forever. tail therefore has to RE-OPEN every poll to observe an appending log, which works (and is what makes tailing correct on ext2, where opens are not exclusive) but is not what `tail -f` actually is: one handle, streaming past EOF. The fix is for the ext2 read path to consult the LIVE `i_size` rather than the snapshot — cheap, and it would also let a reader notice truncation instead of reading into a hole. Booked 2026-08-18 after the evening this cost: on FAT the re-open was being REFUSED (`FF_FS_LOCK` makes a write-open exclusive), which starved tail, and when tail won instead it starved logd until logd gave up and released the log sink. Both symptoms were FAT-only — /home is ext2 now on the P5 AND in QEMU — but the snapshot-at-open behavior outlives them. **Chris's, for a quiet day.** | Feature-gate | S | when someone wants a real `tail -f` | ext2_internal.h `ext2_handle_t` / the tail-vs-logd evening, 8/18 |
| **FAT backward seek is O(position): `FF_USE_FASTSEEK=0`.** Every backward f_lseek rewinds to the file's FIRST cluster and walks the chain to the target — readline's seek-back gait went O(n²) on the 46MB log (4,320 lines/60s, decelerating; the grep autopsy 2026-08-06). os64_linereader cured the LINE-READING case userland-side (forward-only, zero seeks); this row is everyone else — any seek-heavy access to a big FAT file pays it still. Fix: FF_USE_FASTSEEK=1 + a per-FIL cluster link map (cltbl, allocated at open for files past a size threshold) | Perf | S–M | the next seek-heavy consumer of big FAT files (a streaming less would qualify) | ffconf.h line 37 / the grep autopsy |
| **NVMe PRP-LIST path: now LOAD-BEARING (2026-08-06)** — every block-cache line fill is a 128-sector (64KB = 16-page) command, so the once-never-run `prpCount > 2` branch executes hundreds of times per boot and the whole 24+34 suite rides on it (green first boot). Remaining diligence: a deliberate edge test (runs straddling the 2-page and MDTS boundaries, unaligned starts) has still never run ON PURPOSE — cheap insurance now that the branch carries the OS | Robustness | XS | next test-writing pass | the envelope autopsy 8/5; baptized by the cache 8/6 |
| **Caching arc phase 2** (each its own ruling, per the 8/6 phase split): memory-pressure eviction + the os64_memory_t `reclaimable` seat (drop-on-demand must be REAL before `available` counts cache bytes — the allocator discovers pressure holding its irqsave lock, so the eviction hook needs its own design conversation); write caching (deferred: no consumers; the 46MB-copy disk-full adventure = first wild consumer sighted); async readahead (wants completion infrastructure — the NVMe-interrupts era). Update-in-place on write was PROMOTED OUT of this row the night it was written: the first measured ext2 copy showed invalidate-and-refill reading 1.2GB to copy 1.5MB | Feature-gate | M–L | when a consumer demands it, ruling by ruling | block_cache.h phase-2 block |
| **NVMe queue depth: one command in flight, ever** (nvme_do_io submit→spin→reap under ioLock — the 64-deep submission queue stands empty). Evidence from the 8/6 iostat histogram: the ext2 copy's 29,760 writes were 2-block commands paying full serialized round trips each. Fix = pipelined multi-command submission WITHIN a request (submit K, reap all before returning — durability preserved, NOT write caching). Re-measure after cp (1MB buffer, Chris's) + update-in-place land: both may shrink the command count enough to demote this | Perf | M | re-measure first; then the next write-heavy sufferer | the iostat confession, 8/6 late |
| **`top` defeats parking: settle-on-read wakes EVERY core.** `mpAcctSettleAll` (the /proc CPU-time render) IPIs all cores so each settles its in-flight span on its own TSC — correct books, but a PARKED core gets woken just to report "nothing in flight," so `top` shows "0 parked" on an idle machine. Observer effect: the measurement destroys the state it reports (spotted by Chris on the 2-hour VBox e1000 soak, 2026-08-06, idle high-90s%). Fix sketch: settle the books AT PARK TIME — the pass that sends a core into `hlt` charges its in-flight span first (one TSC read, on the core itself, so the no-cross-core-TSC-math law holds); the reader then skips cores sitting in their idle thread and counts them parked. Bonus: parked count becomes an honest first-class stat instead of a self-defeating one | Perf | S | when top's parked count should read true (it just started to matter) | smp_core.c settle-on-read / procfs.c / the tickless default |
| AP sibling starvation — no intra-pass timeslicing under a CPU-bound thread | Robustness | L | when it bites | SCHEDULER #2 / GRAPHICS #10 |
| Move scheduler printds OUTSIDE `kSchedulerSwitchTasksLock` (the convoy: DETAILED must be slow-but-honest, never a lockup) | Robustness | M | none — Chris's "bend to my will" requirement | SCHEDULER #4 / autopsy |
| **Wall-clock hardening — HALF paid 2026-08-10; the IRQ0 half was TRIED AND REVERTED THE SAME DAY.** The prologue reorder (guard before the `temp_rsp` store) shipped and stays. Promoting IRQ0 above scheduler priority (0x20 class 2 → 0xE0 class 14) shipped, froze VBox at boot, and was reverted hours later — full postmortem in the IRQ0_APIC_VECTOR headstone in smp_core.h. Short version: QEMU was perfect (24+19 green, INTx confirmed on GSI 20); VBox's e1000 probe reported EVERY candidate GSI silent and the OS wedged at scheduler start, and a DIRECTLOG boot showed `kTicksSinceStart` climbing to 560 and then all nine probe lines carrying the SAME timestamp, 597, across ~4.5 seconds of wall clock. The clock did not slow, it STOPPED, and every other interrupt with it — the signature of a stuck LAPIC ISR bit, which at class 14 pins PPR and blocks the NIC (class 4), the scheduler (class 7), and IRQ0 itself. The promotion almost certainly did not CREATE that bug; it made an old one lethal, because a stuck ISR bit at class 2 blocks almost nothing and hides indefinitely. DO NOT re-promote the vector: nothing high enough to outrank class 7 can avoid outranking devices at class 4, so the hazard is inherent to the approach, and cure (b) below retires the question entirely. **The tick loss is therefore UNPAID**, with a measured floor: ~6s lost over 290s of periodic-mode uptime under load (top + 4 hogs), and `top`'s clock visibly stepping 2 seconds at a time. Beyond the scheduler-pass losses, the remainder is to **IF=0 windows, where the LAPIC is not arbitrating at all** — dominated by SFMASK clearing IF for the ENTIRE duration of every syscall (smp_core.c ~461), plus the allocator's interrupts-disabled spinlock, plus host vCPU descheduling under WSL2 that no guest-side change can reach. Note the shape: a SHORT window only DELAYS a tick (the IRR holds it, it fires on the way out); only a window longer than one 10ms tick period destroys one, because the IRR holds exactly ONE pending instance per vector. So ~2% loss ≈ two >10ms IF=0 windows per second. TWO CURES, and the cheap one is a patch: (a) lost-tick compensation — `handler_irq0_asm` compares TSC against the last tick and credits the ticks it can prove went missing (Linux carried exactly this for years; S, honest clock, symptom-level); (b) the right answer — stop COUNTING interrupts and start READING a counter: derive wall-clock from the TSC (accounting already uses it) or the HPET, leaving IRQ0 to drive only scheduling cadence, at which point a delayed interrupt costs nothing (M–L, and the same instinct as tickless). The constant ~4s boot offset is a SEPARATE row below (RTC resync, declined: "steady is fine") | Robustness | (a) S / (b) M–L | none — and (b) is now the PREFERRED route, not merely the tidier one: (a) leaves the stuck-ISR hazard undiagnosed and the priority question open | smp_core.h IRQ0_APIC_VECTOR headstone / SCHEDULER #7 / autopsy |
| `kSchedulerSwitchTasksLock`: irqsave + bounded-spin panic tripwire (send_ipi ICR pattern) — also the VBox-freeze hunt instrument | Robustness | S | none | SCHEDULER #8-9 |
| **logd file sink** — at root-mount, logd drains to a file (ramdisk = memcpy-speed, kills steady-state queue creep; NVMe boots get persistent logs). Serial stays for panics/BOOTMARK/early-boot + optional tee. PRECONDITION: FatFs write support is UNVERIFIED ("the FAT library has never written anything" — check FF_FS_READONLY, then a create/write/readback post-boot test BEFORE wiring the sink) | Robustness | M | after hello-userland (Chris's ordering) | log.c / never-drop rule / 2026-07-11 plan |
| ring-3 klogd (userland log daemon reading kernel log via syscall) | Feature-gate | M | post-shell — needs read-from-kernel-log plumbing | LIBOS64/ABI roadmap |
| Resync `kSystemCurrentTime` from the RTC at end of kernel_init — boot tick-starvation currently leaves the OS clock ~3-4s behind forever (Chris declined for now: "steady is fine"; one-liner when wanted) | Cleanup | XS | when the lag annoys | kernel.c / 2026-07-11 |
| `kCPUCyclesPerSecond` miscalibrated ~10× under QEMU/TCG — trust cycle counts, fix the calibration | Cleanup | S | before anything times itself with it | kernel.c tscGetCyclesPerSecond |
| Uncomment the divide-config write in `ap_configure_scheduler_timer` — arming currently depends on the divisor leftover from calibration | Cleanup | XS | none | smp_core.c |
| Allocator first-fit linear scans (merge scans whole ledger; compaction every 10th free) | Cleanup | L | measure before caring | MEMORY #2 |
| IRQ-safe sub-tick wake primitive | Feature-gate | M | only if a real need appears (100Hz covers the GUI) | SCHEDULER #3 |
| Pipe waiter slots are SINGLE (one parked reader + one parked writer per pipe). A second waiter on the same end is still correct — it falls back on its ~1s SIGSLEEP backstop and re-checks — but it is slow. Fix = a real wait list per end | Cleanup | S | when >1 reader or >1 writer on one pipe becomes normal | `pipe.c` |
| Console scroll runs with **interrupts off**: the renderer spinlock is held across a ~3MB full-screen `memmove`, so a scroll can delay that core's tick by a few hundred µs. Correctness beats the jitter (the console is not a hot path), but if it ever matters the fix is a scroll that does NOT hold the lock — not a lock that does not cover the scroll | Cleanup | S | if the console ever gets hot | `BasicRenderer.c` kRendererLock |
| ~~`handle_alloc`/`handle_close` take no lock~~ **PAID 2026-08-25 (PR #29).** The gate this row named — "the day handles are shared between threads of one task" — had already passed when ring-3 threads landed, which is how the two-closers race (Codex #29 rd4) and the two-allocators race (Fable's rd-review) both got in. There is still no lock: the slot's TYPE is an atomic claim token. `handle_close` exchanges live→`HANDLE_CLOSING`, `handle_alloc` CASes `NONE`→`CLOSING` before publishing the real type, and neither touches a slot it did not win. A LOCK is the fallback if this table ever grows an operation that cannot be expressed as one atomic on the type | Cleanup (PAID) | — | done | `handle.c` |

## Threads (os64's first ring-3 threads, 2026-08-02)

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **No PER-THREAD teardown while the task lives.** *(Scope clarified 2026-08-15 — this row is NOT about task memory, which is fully reclaimed: `task_destroy` frees every thread's stacks and `thread_t` at burial, and the VMA reclaim closed the last residual the same week.)* What is still missing is reclaiming a thread that exits while its TASK KEEPS RUNNING: it becomes a ZOMBIE holding ~1MB of user stack + 64KB of kernel stack + its `thread_t` until the whole program exits. Blast radius today is ZERO — every threaded program starts N, joins them, and exits — and it only bites a program that creates threads in a LOOP, which nothing does yet. `thread_join.c:105` still carries the matching comment | Robustness | M | when any program creates threads repeatedly rather than once | `thread_join.c`, `syscall_thread_exit` |
| **No general locks or atomics for PROGRAMS** — deliberate v1 (Chris's ruling): a mutex designed before its first consumer is the wrong mutex. *(2026-08-15: the LIBRARY now has one — `libos64/heap.c` carries a test-and-set + `pause` + `os64_yield` lock, because threads share one address space and therefore one heap, making malloc the first genuine consumer of shared mutable state. It is deliberately private to the heap: a dozens-of-instructions critical section is the wrong customer to design a general mutex around, but it IS the first real datum about what one should cost.)* | Feature-gate | M | the first PROGRAM with genuinely shared mutable state | `os64/thread.h`, `libos64/heap.c` |
| `thread_join_create` leaks the `thread_t` if the stack-mapping check fails (the only failure path after createThread). Cannot currently happen — the stack was just mapped — and fixing it properly needs the thread teardown above | Cleanup | XS | with thread teardown | `thread_join.c` |
| No thread affinity control from ring 3: a program cannot say "put this one on its own core." The scheduler's own delegation handles the useful case today | Feature-gate | S | when a program has a reason to care | `syscall_thread` |

## Floating point (the FPU slice, 2026-08-27 — booked the day it was reviewed)

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **No XSAVE, so no AVX.** The register file is switched as one 512-byte FXSAVE image; `CR4.OSXSAVE` stays clear, so any AVX/AVX2/AVX-512 instruction is `#UD` at ring 3 (which now kills the program, not the machine). Consistent and safe — the YMM upper halves cannot be live if nothing can execute an instruction that reaches them — but it means `-mavx` in any userland build is a crash, and the compiler must never be told the CPU has it. The cure is XSAVE with the variable-size image `CPUID.0xD` reports, a per-boot size instead of a fixed 512 in `thread_t` and both signal frames, and `xsaveopt`/init-tracking if the copy cost ever shows | Feature-gate | M | the first consumer that wants AVX (none in sight — `-msse2` is the baseline every app builds against) | `fpu.h`, `fpu.c` |
| **A CPU exception from ring 3 is a kill, not a catchable signal.** `#DE`, `#UD`, `#GP`, `#MF`, `#XM`, `#AC` end the task with exit `200 + vector` (`user_exception_kill`). Unix answers these with SIGFPE/SIGILL/SIGBUS, and a handler CAN legitimately want them (a calculator recovering from 1/0; a JIT probing instruction support). os64 has no such signals, and adding them to the table before a consumer asks is exactly the speculative growth the roadmap forbids. When one asks: the §9 path (`signal_deliver_segv`) already resumes a caught fault into its handler out of the exception frame, and these would ride it | Feature-gate | S–M | the first program that wants to survive its own FP or illegal-instruction fault | `simple_exceptions.c`, SIGNALS.md §9 |

## Heap (the userland malloc, 2026-08-15 — booked the day it was written)

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **Two threads faulting on the SAME page each resolve it, and the loser's page LEAKS.** The static demand-paging path is "resolve backing page → map it" with nothing serializing the pair, so concurrent faults on one address both `kmalloc_aligned` a frame, both fill it identically, and both map it — the second `paging_map_page` overwrites the first's PTE, and the orphaned frame is now reachable from no page table, which means the burial-time VMA reclaim (which walks the tables) can never free it. Harmless to correctness (the contents are identical), and *rare* — it needs two threads to fault the same page in the same instant — but it is a genuine leak in a kernel that just finished paying off every other one. Found 2026-08-15 while fixing the seek/read race next door; the shared-object path already dodges it with a CAS'd per-page slot (`shared_object_resolve_page`), which is the model for the fix | Cleanup (leak) | S | when a threaded program's first-touch storm shows up in the allocator's numbers | `simple_exceptions.c` handle_page_fault static branch / `shared_object.c` CAS precedent |
| **No APP allocates yet.** The heap ships with no consumer but its own fixture: every utility still carves fixed buffers or maps a whole region per object (`linereader` 64KB per open, `tail` 32KB chunks, `file_pager` the entire file). Converting them is not mechanical — each one gets to become a better program, which is why it is Chris's joy and not a sweep | Cleanup | S each | as each utility earns it | `libos64/heap.c` callers |
| **A big-allocation loop burns address space.** Every allocation ≥128KB takes its own region and gives it back on free, and region VAs are NEVER reused (the deliberate stale-pointer tripwire). A program looping malloc(128KB)/free would exhaust the 47-bit heap range after ~1 billion iterations. Not a today problem — glibc fights the same churn with a dynamic mmap threshold, and the same trick applies here (keep the last freed dedicated region cached for reuse) if a real program ever shows it | Cleanup | S | if a program's profile ever shows region churn | `heap.c` `HEAP_BIG_BYTES` |
| **First-fit is O(free blocks), and `largest_free` rescans when the reigning largest leaves.** Both are fine at the sizes anything here allocates, and both are the price of the ruling that a single free list beats size-class bins for this OS. The day a program holds tens of thousands of free blocks, the bins are still sitting in the design doc | Cleanup | M | if a profile ever shows the free-list walk | `heap.c` `free_list_first_fit` |
| **`realloc` of a DEDICATED block always copies when it grows.** The region could be remapped in place if the kernel grew a `remap`, but it has no such verb and no consumer has asked. Shrinking a dedicated block is free (the region already covers it) | Cleanup | S | when a program reallocs big blocks in a loop | `heap.c` `os64_realloc` |
| **`HEAPCHECK=1` verifies every canary on every call — and nothing measures how slow that is.** It is the bug-hunt gear (MALLOC_CHECK done honestly), not a default; worth a number next to it so a future hunter knows what they are buying | Cleanup | XS | next time it gets used in anger | `heap.c` `gCheckAlways` |

## Kernel structure

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **Thread kernel stacks belong in kernel space — retire the per-core scratch stack.** The stack poisoner (2026-08-12: two contexts colliding on one per-core "kernel_interrupt_stack") existed only because thread RSP0 stacks live at task-local lower-half VAs invisible to kKernelPML4, forcing every CR3-crossing path (task_exit's last breath, call_in_kernel_context's file/dir closes and shared-object page reads) onto a borrowed, bolted-to-the-core slab. Moving RSP0 stacks to kmalloc-backed upper-half memory (valid under EVERY CR3, use-after-free tripwired by the lazy HHDM like everything else) retires the slab, the cikc stack-switch, AND the whole "no sleeping on a borrowed stack" rule — a context would always stand on its own traveling stack. Until then: the teardown split (task.c) cures the exit path, and the borrowed-stack dispatch tripwire (scheduler.c) names any door that still swings. Also rename `kernel_interrupt_stack` — no interrupt has ever loaded it; it is the CR3-refugee scratch stack, and the name misled a two-day investigation | Robustness | L | a design ruling first (isolation doctrine vs kernel-resident thread stacks) | scheduler.c borrowed-stack tripwire / task.c teardown split / PAGING_ARENA.md's sibling doctrine |
| **EXCOLD and the old exception stubs retire together** (2026-08-11, the unification). The old per-vector stubs (handler_errors.S minus the NMI gate), the old reporters (`exception_panic`, `dump_fault_registers`, `dump_stack_trace`, the `gLastFault*` globals), and the deliberately-duplicated print/sanity helpers in exception_report.c all exist only to keep the EXCOLD fallback bootable. The duplication is the fallback's armor (a shared helper could break both paths at once) and dies with it. Retire after the unified path has survived a real soak AND a real fault on the P5 | Cleanup | S | unified path earning trust on bare metal | exception_report.c header note / kUseOldExceptions |
| **The demand pager still lives in simple_exceptions.c** — `handle_page_fault` plus its CoW/shared-object/user-kill halves belong in their own `page_fault.c`, with simple_exceptions.c shrinking to the EXCOLD museum piece. Deferred from the unification ON PURPOSE: a pure code move in the same change as a behavior change means two suspects when something breaks. Do the move alone, diff-only, after EXCOLD retires (one move instead of two) | Cleanup | S | EXCOLD retirement (the row above) | simple_exceptions.c |
| **`noseriallog` is a documented flag that does nothing.** The token rides in limine.conf's `/No Serial Logging` entry and was documented in CLAUDE.md as real, but it appears in no parser table and nothing outside kernel_commandline.c so much as reads the cmdline — the entry has always booted with serial logging fully on. Found 2026-08-16 by sweeping every limine.conf token against the parser table (the same sweep caught the lowercase NOAHCI/NONVME entries, which ARE now fixed). Chris's ruling 2026-08-16: book it and correct the docs rather than delete the entry — the entry is the feature request. **SUPERSEDED 2026-08-20**: the limine.conf walk deleted `/No Serial Logging` along with every other entry that carried a single bare token and no `ROOT=` (none of them mounted a root or reached husk). The feature request is not lost — it lives in THIS ROW now, which is the more honest place for it, since a boot entry that silently does nothing is a worse marker than a ledger line that says so. Real shape when built: a `kSerialLogging` bool the logd drain honors, DIVERTING rather than discarding (a file sink still gets every byte — "never drop a byte" is not negotiable, and a mute switch that eats log lines would be the exact thing the motto forbids) | Cleanup | S | when someone wants a quiet wire with a live file log | kernel_commandline.c cmdopts table (the limine.conf entry that used to carry it was retired 2026-08-20) |
| **`syscall.c` does too much work.** Chris's os32 principle (2026-08-02): the syscall file should DISPATCH — validate arguments, copy user memory, call the subsystem that owns the verb — with cases of five or ten lines, not implementations. os64 has drifted in layers: the newest syscalls already delegate (`net_dial`→`tcp_conn_dial`, `klog_read`→`klog_dequeue`, `thread`→`thread_join_create`), while the oldest carry their work inline (`read`/`write` own all the chunking and bounce-buffer discipline; `spawn` builds its own parameter block). The seam is obvious when it's worth cutting: boundary work stays at the door, verb work moves to the owning subsystem | Cleanup | L | explicitly LOW priority — "a working OS with more syscalls than its ancestor has earned a fat dispatcher" | `syscall.c` / os32 comparison |
| **synthfs renders every file into ONE contiguous buffer** — `synth_text_grow` doubles by `kmalloc(newcap)` + copy, and the allocator never returns NULL (exhaustion panics by name), so a synthetic file whose text runs to megabytes is a contiguous-allocation demand made on every `cat`. Every /sys and /proc file shares it; `/sys/net/tcp` is the first whose table can get there (16384 rows ≈ 1MB at the full ephemeral range) and where Codex flagged it (PR #46 rd7) after the row SNAPSHOT had already been chunked into pages for the same reason. Capping the table was rejected: it would blind netstat exactly under the load it exists to show. The fix is synthfs's, not one file's: page-chunked text (a `read` walks the chunk list by offset) or a streaming generator, so no synthetic file ever needs more than a page contiguous | Robustness | M | before any synthetic file routinely renders past a few hundred KB | synthfs.c `synth_text_grow`, sysfs.c `sys_gen_net_tcp` |

## Filesystems (the rename verb, 2026-08-16 — booked the day it was written)

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **`open` cannot say "not found".** SYSCALL_OPEN answers one value — SYSCALL_RESULT_INVALID — for a missing file, a malformed path, a full handle table and "nothing mounted yet" alike, so no ring-3 reader can distinguish ABSENT from UNOPENABLE. First consumer: `os64_slurp` (Codex #30 rd10), whose NO_FILE had promised "nothing is there, a fresh write is safe" — a promise it could not keep, now withdrawn in slurp.h. The shape when built: a distinct not-found result (an OS64_ERR_* the way timeout got one, since these cross the ABI), plumbed from vfs lookup through syscall_open; then slurp's NO_FILE splits into NO_FILE (absent, write-safe) and OPEN_FAILED, and image.c's status names can say "no such file" again | Correctness | S | the first caller that decides whether to WRITE on the strength of NO_FILE (conf writers already write to /home unconditionally, which is why nothing is wrong today) | slurp.h NO_FILE paragraph; syscall.c syscall_open return sites |
| **`mount.h` PUTS ITS OWN MEANING ON A NUMBER THE SYSCALL BOUNDARY ALREADY OWNS** — the row above's problem seen from the other end. `SYSCALL_RESULT_INVALID` is -1 and `SYSCALL_RESULT_BAD_USER_DATA` is -2 (syscall.c), and any family publishing its own negative codes in an abi header shares that number space without either side knowing. **`os64/net.h` is the house pattern and says so out loud** — "The first two are the syscall boundary's house-wide verdicts, named here so a dialer can read them; the rest are dial's own" — reserving -1 and -2 and starting dial's own codes at -3. `mount.h` did not: `OS64_MOUNT_BAD_ARGS` is -1 (harmless, it happens to coincide) but `OS64_MOUNT_NOT_FOUND` is -2, so a courier returning BAD_USER_DATA made `mount(1)` print "no partition wears that name or GUID (see /sys/block)" for an argument that was merely unreadable or over-long — accusing a disk of missing a partition and sending the reader to the wrong file to check. Found 2026-09-01 reviewing the round's own fixes. The PRACTICAL hole is closed (8c08109: the two couriers answer argument errors in mount.h's vocabulary, so no -2 leaves those paths, and the collision is written down where they return), but the DIVERGENCE stands — any future mount-family path that returns BAD_USER_DATA reads as NOT_FOUND again, silently and only in the error message. Fix = renumber so -2 carries the boundary's meaning and the mount family's own codes start at -3; in-tree that is mount.h, the kernel's returns, and `mount(1)`/`unmount(1)`'s tables, which are the only readers | Robustness | S | none — but it is an ABI renumber, so do it when nothing is mid-review against the old numbers | `abi/include/os64/mount.h` vs `abi/include/os64/net.h` |
| **FAT's rename is NOT atomic.** The seam promises that a replaced destination never stops resolving; ext2 keeps that promise with one directory-block write, but FatFs's `f_rename` refuses an existing destination outright, so `fat_rename` does remove-then-rename and there IS a window where neither name exists. Not fixable inside FatFs's API — FAT has no file identity separate from the directory entry (that idea IS the inode). Root is ext2 precisely so the guarantee lives where it can be kept; the FAT lifeboat is documented, not repaired | Feature-gate | L | only if FAT ever has to be a safe-publish target — i.e. probably never | `fat_glue.c` fat_rename |
| **A crash mid-rename leaves ONE SPARE LINK for e2fsck.** Deliberate: `ext2_rename` raises `i_links_count` before publishing the new name and lowers it after removing the old, so every crash window has count ≥ names, never fewer. Over-counting is the safe direction (fsck corrects it; an under-count lets the allocator hand out a live inode), but it does mean an unclean shutdown during a rename leaves a filesystem e2fsck will *fix* rather than *pass*. The real cure is a journal, which os64 does not have and is not pretending to | Robustness | XL | if os64 ever wants crash-consistency without fsck (i.e. ext3's whole reason for existing) | `ext2_write.c` ext2_rename step ordering |
| **The namespace lock is a plain spinlock held across a whole mount.** `kNamespaceLock` (vfs.c) makes mount and unmount exclusive, and its critical section contains the driver's `initialize`/`uninitialize` — real disk I/O, milliseconds, not microseconds. spinlock.h's own contract says the plain variant is for sections that are SHORT or callers that are RARE, and this leans entirely on the second: a contending core spins for the length of somebody's mount. That is affordable while these verbs are human-paced (typed at a shell, or a handful of `/etc/mounts.conf` lines at boot) and stops being affordable the moment anything automated mounts in a loop — a hotplug responder, or an auto-mounter. The upgrade is the one spinlock.h already names: a sleeping mutex, which is also what the exclusive half of the path gate would want. Not built now because os64 has no sleeping mutex at all, and inventing one under a bug fix is how a fix becomes an arc | Performance | M | the first caller that mounts without a human waiting for it | `vfs.c` kNamespaceLock / spinlock.h plain-variant note |
| **A renamed directory strands any task whose cwd is inside it.** `task->cwd` is a STRING, so renaming `/a` to `/b` leaves a task sitting in `/a/x` holding a path that no longer resolves — every relative open from that task then fails. Unix does not have this problem because a cwd there is a reference to the directory itself, not to its name. Fix = hold something identity-shaped (inode number + filesystem) rather than text, which is a real change to how cwd works everywhere | Robustness | M | when someone actually renames a directory out from under a running shell (husk, most likely, and it will be confusing) | `task.h` cwd / `ext2_rename` |
| **Cross-filesystem rename is refused, and nothing does the copy instead.** `syscall_rename` correctly declines when the two paths land on different mounts (that's a copy, not a rename) — but until `mv` exists in userland to do copy-then-unlink, moving a file from `/home` to `/` simply cannot be expressed. Not a kernel debt so much as a note that the userland half is what completes the verb | Feature-gate | S | when `mv` gets written (Chris's, per the labor division) | `syscall.c` syscall_rename cross-mount refusal |
| **~~A failing `close()` cannot tell anyone~~ — KERNEL HALF PAID 2026-08-24; what remains is one ABI RULING.** The silent-loss half is fixed: `handle_file_object_close()` returns the filesystem's status, `file_close_in_kernel()` carries it back through a params block, `fat_close` reports it (and no longer leaks its two allocations on the failure path), and a failed flush is logged LOUDLY at the source under `DEBUG_EXCEPTIONS` — always-on, deliberately, since a tripwire tagged with a flag that is off by default is a check that cannot fire. Every closer now learns, including the burial closer in task.c that has nowhere to return an error to. **STILL OPEN, and it is a decision rather than a defect: what should `close()` RETURN TO RING 3 when the flush failed?** POSIX says the descriptor is gone regardless and the error is informational, which os64 should probably match — the handle must be released either way or a program leaks handles by failing — but "released, and by the way your data may not be on disk" needs a distinct answer that is not `SYSCALL_RESULT_INVALID` (that already means "no such handle", and conflating them tells a caller to go looking in the wrong place). `os64_conf_write` does not need it — it commits through `os64_sync`, which does propagate — so nothing is blocked on this | Feature | S | when a caller other than conf needs to ACT on a failed flush, rather than merely have it recorded | `kernel/src/handle.c` handle_file_object_close / `syscall_close` / ABI |
| **"Read a whole file up to a cap, and say whether it was bigger" EXISTS THREE TIMES, and every fix to it has had to be applied three times.** The copies: `kernel/src/conf.c` (`conf_io_kernel`), and `os64_conf_read` + `os64_conf_write` in `userland/libos64/conf.c` — the last two in the SAME FILE, twenty lines apart. This is not a suspicion, it is the review record: PR #29 rd1 fixed truncation-reported-as-success in two of the three; **rd3 was the third copy, missed by rd1**; rd12 fixed read-error-treated-as-EOF in all three at once. Three separate rounds spent on one bug in n places. That is what the config subsystem has actually been "screaming for" across thirteen review rounds — not more care, less arithmetic. THE CURE HAS PRECEDENT IN THIS TREE: `klog_format.h` holds the ONLY copy of the log layout logic precisely because it used to be spelled four times across the ring boundary; same disease, same shape of fix. Minimum viable payment is to collapse the two USERLAND copies into one `conf_slurp()` (same file, no ABI surface, pure subtraction); the kernel's stays its own because it is a different ring with different I/O, but then there are two that look alike instead of three that drift. Booked at Chris's call 2026-08-24 rather than done inside the signals PR — it is a refactor, and refactors do not belong in a review gauntlet. **THE HELPER NOW EXISTS: `os64_slurp()` (userland/libos64/slurp.h + .c, born 2026-08-25 with libimage), carrying all four distinctions and the receipts for each. The remaining payment is ADOPTION** — the two conf.c copies calling it instead of their own loops. Deliberately not done in libimage's PR: rewriting the most-reviewed code in the tree does not belong in a slice about pictures. **AND THE COUNT WAS WRONG — IT WAS FOUR COPIES, NOT THREE.** `kernel/src/gui/desktop.c`'s `read_whole_file` was the fourth, written before this row's "before a fourth reader" gate and never counted because it was named differently. It carries the rd12 bug UNFIXED to this day (`if (n <= 0) break;` — an error read as an ending); the consequence is mild (a truncated wallpaper fails to parse, nothing is written) which is exactly why nobody noticed. **It leaves with the desktop shell** in the ring-3 move, which is why it is not being fixed in place — but if that move stalls, fix it there | Robustness | M | ADOPTION next time conf.c is opened; desktop.c's copy dies with the ring-3 shell | Codex #29 rd1/rd3/rd12; `kernel/src/conf.c` + `userland/libos64/conf.c` + `gui/desktop.c` |
| **`open` has no EXCLUSIVE CREATE, so nothing in os64 can CLAIM a name.** Every mode either tolerates an existing file or destroys it: `"w"`/`"c"` are create-**or-truncate**, `"a"` appends, and `rename` **replaces** — the very property that makes atomic publish work is what makes it useless as a claim. There is no `link()` either. So the "create it only if it isn't there, and tell me which happened" primitive that lock files, `mkstemp`, and every once-only initialisation are built on simply does not exist. Wanted shape when it lands: a mode that FAILS when the target exists (tripwires over silence — the answer must be distinguishable from every other open failure), and it belongs to `open` rather than a new syscall, since it is a property of the opening | Feature | M | when a consumer needs to claim a name — the config lost-update below is the first, and it is not urgent | `abi/os64/syscall_numbers.h` #9 modes / `os64/io.h` |
| **Two concurrent saves of ONE config file lose one of them (a lost UPDATE).** `os64_conf_write` is read-modify-rename and the middle is unserialised: two savers read the same old text, each merges its own setting, and whichever renames LAST silently discards the other's. The corruption half is already fixed — per-saver temporaries (Codex #29 rd7/rd8) mean nobody publishes an inode another writer is still filling — so what remains is purely the lost update. **Gated on the exclusive-create row above**, because there is nothing to build the fix out of today: a retry-on-changed-source loop narrows the window without closing it, and a guard that is almost right invites trust it has not earned. Chris's ruling 2026-08-24, deferring it whole: with os64's current codebase and userbase the odds are "approximately 0.00000001%" — no consumer saves the same file from two processes, and the ones that save at all (gclock, the desktop) each own their file | Robustness | M | exclusive create first; then a real claim around the whole merge | Codex #29 rd9 / `userland/libos64/conf.c` os64_conf_write |
| **/dev block nodes — the day raw disk I/O becomes ordinary file I/O.** The NAMES are already committed (`block_assign_dev_names`: `nvme0`/`sata0`/`ram0`, printed by /sys/block — /dev nodes MUST reuse them verbatim, that commitment is the whole reason they were settled early). Reads are cheap; WRITES need a designed answer to `block_verify_write_allowed` (a raw write to a mounted partition is exactly what the stray-write tripwire exists to stop, and also exactly what dd is for — "unmount it first" is the obvious v1 policy) plus block-cache coherence. First customers: hexedit on a partition, dd, the native fsck above, reflashing the lifeboat from inside os64 | Feature | M | after mount/unmount ships; hexedit-on-a-partition is the pull | the 2026-08-30 mount design conversation |
| **USB mass storage + ISO9660 — thumb drives mount like disks.** `usb0` is the fourth bus prefix when xHCI grows bulk transport. ISO9660 enters the way every filesystem enters os64 now — something recognized on a block device in the sweep — NOT welded to ATAPI/CDs (there will be no CDs, Chris 2026-08-30). Wrinkle worth remembering: a stick imaged from an ISO has no GPT — whole-disk filesystem recognition is part of the slice | Feature | L | when a thumb drive matters | Chris, the 2026-08-30 mount conversation |
| **FatFs volume numbers leak on non-LIFO unmount.** `kFatDiskNumber` only reuses the TOP number (unmount handles LIFO); mount A, mount B, unmount A leaks A's number until reboot, bounded by FF_VOLUMES (10). Loud when it finally bites (f_mount refuses); a free-number bitmap is the cure if mount/unmount churn ever becomes a workload | Cleanup | S | if FAT mount cycles become routine | vfs_unmount |

## Userland utilities

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **tar's recursive safety relies on a namespace with no links.** Its self-exclusion can identify the output archive by canonical path text because one file cannot have a second name, and extraction's lexical absolute/`..` gate is sufficient because no earlier member can install a symlink that redirects a later one. Link support changes both facts: hard links require file identity for self-exclusion, while archive creation must not follow symlinks and extraction must either refuse link members or resolve every destination beneath the extraction root without following installed links. This is a feature gate, not a defect in today's namespace; the link slice must update tar in the same change | Feature-gate | M | before either hard or symbolic links enter the namespace | `tar.c` canonical_path / `tar_format.c` tar_extract_path / `dirent.h` symbolic-link warning |
| **whois follows no referrals.** IANA answers a domain query with the TLD's registry in a `refer:` line, the registry answers with the registrar, and a full whois chases that chain until a server has no `refer:` left (GNU whois carries a static table of servers per TLD instead — a maintenance burden this house does not want). v1 prints IANA's answer raw and `-h SERVER` walks the chain by hand. Booked at birth (BROWSER.md rung 2 said "book it, don't build it"): the chase is a loop over the same dial-send-read the program already does, plus parsing one `refer:` line and a hop limit so two servers referring to each other cannot spin | Feature | S | when a person tires of typing `-h` | `whois.c` main |
| **POSIX ustar cannot record a file of 8 GiB or larger.** Its 12-byte size field carries 11 octal digits, so tar now refuses above 8 GiB minus one byte with a format-specific diagnostic instead of the codec's generic numeric-field error. GNU's base-256 size encoding is the compatible extension when a real file reaches that ceiling; it belongs in both encoder and decoder, with host interop at the boundary | Feature | S | the first archive containing a file of 8 GiB or more | `tar.c` write_header / `tar_format.c` put_octal and get_octal |
| **A fixture's SKIP code cannot tell "this boot can't" from "this code is broken".** `synctest` reports `0x05CC0001` and `conftest` reports `0x0C0F0001` when a write to `/home` fails, and testrun treats both as SKIP — correctly, because a boot with no writable `/home` is a fact about the boot and a suite that cries wolf gets ignored. But the SAME code is what a genuinely broken write path would produce, so the day `os64_conf_write` or `sync_all` actually breaks, the suite says SKIP and goes green. The cure is for the fixture to establish WHY before deciding: probe `/home` for writability first and report "no writable /home" as its own distinct code, leaving the original code to mean what it says. Cheap, and it wants doing once for both fixtures rather than twice. Booked at Chris's request 2026-08-23 after `conftest` failed a run that had no data disk attached; low priority precisely because a boot without `/home` is not the normal case, which is also what makes it the kind of masking nobody notices until it matters | Bug (test-only) | S | before trusting a green suite on an unusual boot — or the next time either fixture is opened | `testrun.c` skipcode column / `conftest.c` / `synctest.c` |
| `tail`: a SHORT READ inside the backward block scan silently loses lines. If the fill loop ends with `filled < blockSize`, only the bytes that arrived are scanned but `blockEnd` still moves a whole block, so newlines in the unread tail are never counted — fewer lines printed than asked, no error. Needs an I/O error or a mid-read shrink to trigger. Fix = treat a short fill as an error, or rescan from `blockStart + filled` | Cleanup | XS | when a real short read is ever observed | `tail.c` find_tail_start |
| `tail` reads no STDIN — `something \| tail` exits 2 instead of following the pipe. A pipe can't seek, so `-n` would need a keep-the-last-N ring instead of a backward scan; that is the actual work | Feature-gate | S | when a pipeline wants it | `tail.c` main |
| `tail` computes "the last N lines" from the `stat` size taken BEFORE the open, so a file that grows in between is measured slightly stale. Self-corrects instantly under `-f`; harmless otherwise | Cleanup | XS | never, probably | `tail.c` main |

## husk scripting (the control-flow slice, 2026-08-28 — booked the day it was written)

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| ~~**No `for`, because husk has no variables.**~~ **PAID 2026-08-28, the same evening, by the ruling it was waiting for**: a variable IS an environment variable (DIVERGENCES § The shell). `NAME=value` is a statement, `for NAME in WORDS` assigns each word the same way, `for NAME` walks a script's arguments, and `export` survives as the older spelling of the same act | Feature (PAID) | — | done | husk run_assignment / exec_for |
| **No `break` / `continue` / `case`.** `break` and `continue` are an unwinding verdict the evaluator does not carry yet (it carries `exit` and abort); `case` is a whole second matcher. Bourne had all three in 1977; nobody here has wanted one yet | Feature | S / M | a script that needs to leave a loop early | husk exec_while |
| **A `while` loop that spawns nothing cannot be stopped from its own keyboard in a script.** Interactive husk polls the console between iterations for a Ctrl+C typed while husk itself was foreground (a child gets it as SIGINT and dies 130, which aborts the block). A SCRIPT does not poll — its stdin may be data, and polling would eat it — so a script's loop is interruptible only through a child. `while true; do :; done` with no child in it is a runaway; kill it from another VT. Gate: a way to ask "is a Ctrl+C pending?" that does not consume stdin (the tty owner could answer it) | Robustness | S | the first runaway script loop | husk loop_interrupted |
| **Tab completion reads the line raw**: no quotes, so a name with a space in it is not completable, and the word is cut at the last blank. Also lists everything at once rather than on a second Tab, and offers no completion for `$NAME` | Polish | S | the first file with a space in its name that someone wants to complete | husk complete_at_caret |
| **A builtin's output inside `$(...)` is not captured.** Builtins write to husk's own stdout, not through the capture pipe; `$(dirs)` prints to the screen and substitutes nothing. Redirecting husk's own handle 1 for the duration would fix it and is a small change; nobody has wanted `$(dirs)` yet | Polish | XS | a script that wants a builtin's answer as a word | husk run_substitution |
| **`time ! cmd` times but does not negate.** Both are prefixes handled in run_segment, `time` first, and `time` hands the rest to run_expanded which knows no prefixes. `! time cmd` works. Bourne treats `!` as part of the pipeline grammar so either order works there | Polish | XS | someone typing it the other way round | husk run_segment |

## GUI (all GRAPHICS.md "future work")

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| Tickless + GUI coexistence — add the damage-wake nudge IPI in `gui_damage_add`, lift `gui_start()`'s pin refusal. Grew teeth 2026-08-05 when tickless became the DEFAULT; **the acute half HEALED 2026-08-13** (the backstop arc's preemption lease — GUI entries dropped `SCHED=periodic` and boot the default scheduler with `BACKSTOP=10`, verified same day). The chronic half remains: a publish's damage reaches the compositor only when an input IRQ or the backstop cadence happens to end its hlt — no wake of its own — and the compositor still declines its core-1 pin under tickless | Feature-gate | S | when GUI latency on a QUIET system matters (the nudge); when pinning proves worth having back | GRAPHICS #1 / SCHEDULER #6 |
| **`ls` assumes a 100-column terminal and no terminal can tell it otherwise.** `%-20.19s` five to a row is exactly 100 columns, and gterm's grid is exactly `WANT_COLS 100` — so every `ls` row fills the grid, the EAGER wrap in `tty.c` moves to the next line, and `ls`'s own `\n` leaves a blank one. Three separable fixes, smallest first: (a) DEFERRED WRAP — a `pending_wrap` flag in `tty_t`, set instead of wrapping at the last column and honored by the next PRINTABLE character, which is what every terminal since the VT100 does and why `printf("%80s\n")` does not double-space on an 80-column xterm; this alone fixes the symptom at any width, on gterm AND the text console. (b) a way for an app to ASK its terminal's size, so `ls` computes columns instead of assuming five. (c) gterm resizing its pty grid on WINDOW_RESIZE plus a notification — booked in **PTY.md's Resize row** (this used to point at GRAPHICS.md for a "SIGWINCH-shaped seam", which was never written there; GRAPHICS.md § Event delivery now carries the signal-vs-event RULE, and PTY.md carries the slice). Note (b) and (c) are separable and (b) is the one `ls` needs: a signal saying "it changed" is useless to a program with no way to read what it changed TO. **(b) and (c) PAID 2026-08-25**: `/proc/self/tty` already printed `rows`/`cols` (the VT arc) — it reported 38x100 on a maximized gterm only because nothing ever told the pty, and `pty_resize` + SIGWINCH now do (PTY.md § Resize; `winchtest` reads the size back through that file). **(a) remains open** — it is the one producing the blank lines, and it is the smallest | Bug (a) | (a) S | (a) now — it is the visible one | `tty.c` wrap block / `ls.c` print_entry |
| Legacy text console (`BasicRenderer.c`) has no SMP locking | Robustness | S | if legacy console is used under SMP (GUI path doesn't) | GRAPHICS #9 |
| ~~**The Alt+Tab SWITCHER — a visible strip of titles, nothing moves until Alt is released.**~~ **PAID 2026-08-23, same day, by Opus — and VERTICAL, not a strip of cells.** Chris's question on reading the design ("what's in each box? the name of the app?") exposed the layout error: a horizontal row of cells works for ICONS, and os64 has none — sixteen titles sharing 1024px get five characters each. A vertical list of titles is both the arithmetic answer and the older Unix one (twm's window menu 1987, fvwm's `WindowList` 1993). Built as designed otherwise: compositor-owned scene layer, snapshot carrying titles + dim flags, nothing moves until commit, Escape cancels, Up/Down walk it too, a VT switch away abandons the hold. `wm_touch_focus` deleted with its reason. **Found on the way, before it shipped: `ascii == 0x1B` DOES NOT MEAN ESCAPE** — every arrow key arrives as a three-event VT100 burst whose first event carries 0x1B, so Escape-cancels would have fired on the first third of every Up press; `keyboard_is_escape_key` / `keyboard_arrow_updown` join `keyboard_fkey_number` as the dialect-aware helpers. Verified by QMP screendump across seven cases, zero panics. The old row read: Today's Alt+Tab raises every window it steps through (the raise IS the feedback) and, worse, RESTORES every minimized window it passes — Chris minimized everything and could not get one back without bringing them all back (2026-08-23, first afternoon with minimize; Fable's "stays restored" rule was the mistake). Design is complete in GRAPHICS.md § "The Alt+Tab switcher": a compositor-owned strip (the rubber band's precedent — not a window), titles as the icons, minimized entries dimmed, Escape cancels, commit on release touches exactly one window. **Deferred to Opus the same day** (the morning's review fan-out cost 7% of the week's Fable quota) | Feature | — | PAID | GRAPHICS.md switcher chapter |
| ~~**The config SEARCH PATH — one `conf = dir:dir` line in `/etc/os64.conf`, one walker, every reader obeys, `/sys/conf` reports which file won.**~~ **PAID 2026-08-23, same day as the design.** All six private ladders gone (logd, husk.rc, os64get, hosts, net.conf, desktop); `kernel/src/conf.c` is the one walker; `/sys/conf` publishes the ladder, its source, and which file each reader took; every resolve prints one DEBUG_BOOT line naming the misses. TWO DEPARTURES from the design, both argued in the doc: ring 3 asks the KERNEL to walk (`SYSCALL_CONF_RESOLVE`, 47) rather than parsing `/sys/conf` itself — the design's route was two parsers of one ladder plus a separate reporting channel, and the syscall makes the per-reader diagnostic free; and the walk is RESUMABLE (`from` position, returns index+1) because `hosts` is MERGED by Chris's 2026-08-22 ruling and first-hit-wins would have silently overturned it — a settings file is not a database. Proven by flipping `conf = /etc:/home` and watching husk move from `/home/husk.rc` to `/etc/husk.rc` | Cleanup / feature | — | PAID | docs/conf_path.md |
| **ABI.md's syscall inventory stops at 43.** 44/45/46 (`pty_create`, `pty_snapshot`, `tty_handle`) were never added to the table, and 47 (`conf_resolve`) was deliberately not added on its own — one row does not make a stale table complete, and a table that looks complete while missing four entries is worse than one that visibly stops. `abi/include/os64/syscall_numbers.h` carries the full, documented inventory and is the authority. Backfill the four together | Docs | S | next time ABI.md is opened for anything | ABI.md / syscall_numbers.h |
| ~~Damage rect LIST instead of single union~~ **PAID 2026-08-18** (compositor.c: 8-rect list, merge-when-cheap heuristic with a 128px slack, overflow falls back to the v1 union so the worst case is exactly the old behavior). THE LIST CARRIED A PREREQUISITE nobody had written down: `composite_one` painted each window's WHOLE frame, harmless under one damage rect and corrupting under several (paint outside rect i lands in rect j, already composited and not yet flushed). Chris's P5 found it within the hour — console background bleeding into gbounce on raise, and trailing the ball only while the mouse moved over the console. Fixed by compositing through a `surface_view()` of the damage rect, so surface.c's existing per-primitive clipping makes "writes stay inside the rect" structural instead of remembered; reproduced and confirmed with a deliberately re-broken control build (2562 bleed pixels vs 0). It is also a second win in its own right: a 25x25 ball no longer repaints the 768x480 console beneath it. Measured in QEMU, same layout and workload both sides: guicomp **18% → 7%** with one animating ball and `top` running. NOT region algebra, deliberately — an exact non-overlapping cover costs more to maintain than the overdraw it saves at this population size; revisit only if rect counts ever get interesting | Cleanup | — | PAID | GRAPHICS #4 / Chris's P5 GUI shakedown |
| PAT write-combining for the framebuffer flush (replaces PCD/UC mapping) — **"measure first" is satisfied**: the P5 pays retail for UC writes and guicomp pegs a core under one animating window (2026-08-17). **Re-ranked 2026-08-18**, deliberately NOT taken with its two neighbours: this is the only one of the three that leaves the compositor — per-core `IA32_PAT` MSR setup in `ap_initialization_handler` plus PTE PAT-bit plumbing in paging.c, kernel-wide blast radius — and the other two just cut the number of bytes it would be a multiplier ON (flushes/sec 33 → 1 with the covered window; the surviving flushes are small rects). Take it when the P5 says the remaining flush volume still hurts, not before | Cleanup | M | after a P5 re-measure with the damage list + occlusion in | GRAPHICS #3 / Chris's P5 GUI shakedown |
| **Occlusion — half paid.** ~~(1) compositor-side~~ **PAID 2026-08-18**: `gui_window_publish` asks `wm_rect_is_occluded()` and drops the damage when the rect is entirely behind one window above it, so no damage → no composite → no flush. Chris's acceptance test ran green in QEMU: dragging the console over gbounce took guicomp **7% → 3%** and flushes **33/sec → 1/sec** (the survivor is `top`'s once-a-second console repaint), and uncovering it brought the ball back live and correct — the snapshot still happens under occlusion precisely so a reveal never shows a stale frame. KNOWN LIMIT, chosen: single-window containment, so two windows that JOINTLY cover a third still composite it; real region subtraction is its own slice. ~~**(2) toolkit-side**~~ **PAID 2026-08-27 — by the OTHER shape** (GRAPHICS.md § Covered windows): the kernel keeps a `COVERED` flag per window (`wm_cover_sweep_locked`, every compositor frame) and nudges with COVERED/UNCOVERED events; `os64_frame_clock_bind` makes the frame clock nap while covered, so every clock-paced app went quiet with one line and gterm stops painting on the event. That is X11's VisibilityNotify with the cooperation moved INTO the library, not the Wayland frame-callback this row sketched — a publish-ack is still the more general design (idle-by-default for apps with their own loops), and it stays available if a consumer outgrows the flag. Proven in QEMU: fpu_demo behind gterm reads 0% and its TIME stops; the eleven-gbounce P5 run is Chris's to repeat. What the measurement had found, for the record: Chris's P5, 2026-08-18: eleven gbounce windows lined up to vanish behind the console. guicomp went to a flat **ZERO** and the machine read 100% idle — the compositor half has nothing left to give — while each hidden client kept accruing **1-2ms** of CPU. That residue is exactly two things, both outside the compositor's reach: the client still computing and painting frames nobody can see, and publish's canvas→content snapshot, which deliberately still runs so a reveal repaints from a CURRENT frame (deferring it to reveal-time would tear, which is why snapshot-on-publish exists at all). The fix is a publish acknowledgment a client can wait on — libui paces app frames on it, Wayland's frame-callback insight (X11's VisibilityNotify asked apps to cooperate and they never did; callbacks make idle the DEFAULT). ACCEPTANCE TEST, better than the original: eleven hidden gbounces, and the per-client ms must stop climbing | Perf | rides libui (2) | (2) Phase C/D's frame-loop design | Chris's occlusion question 2026-08-17; his eleven-window P5 measurement 2026-08-18 |
| **Live theme reload.** libui reads `/home/theme.conf` once, at app init — a freshly picked theme applies to NEWLY launched apps, not running ones, so the coming theme picker (his app; activation = copy `/home/themes/<name>.conf` over `theme.conf`, Unix all the way down) ships restart-to-apply v1. The live version needs a "theme changed" nudge running apps can hear and repaint on — the SAME event-to-client machinery as the publish acknowledgment in the occlusion row (Gnome 2 did it with a signal; X11 made apps watch xrdb and most didn't). Design them together: one client-notification seam, two customers on day one | Feature-gate | S (rides the frame-callback seam) | with the occlusion row's publish-ack design | Chris's theme-picker morning, 8/19 |
| **Window resize — PAID 2026-08-19** (Ctrl+Alt+right-drag with a rubber band and quadrant anchoring; Ctrl+Alt+left-drag moves). Close / minimize / honor `GUI_WINDOW_NO_DECORATIONS` remain open: those are CHROME features, and this slice deliberately added no chrome at all — which is why it could ship without touching `composite_one` | Feature-gate | S (what's left) | UX | GRAPHICS #5 |
| **Canvas capacity is committed eagerly** — a window reserves and maps a SCREENFUL of canvas (and a screenful of kernel-side content), not the size it actually is. That is exactly what buys the resize contract: no allocation, no pixel motion, and never an unmap of a live user page. **MEASURED 2026-08-19** (Chris asked whether the estimate was real, having found 0x301000 of canvas in gterm's `/proc/<pid>/status` and reasonably concluded the cost was 3MB): a windowed task costs **7,616,144 bytes**, a windowless one **1,306,808**, so the window itself is **6,309,336** — exactly two capacity surfaces (2 × 3,145,728 at 1024x768) plus ~17KB of page tables for the task mapping and the `window_t`. The same gbounce under the old content-sized allocation carried ~474KB of surfaces, so its total footprint went 1.76MB → 7.62MB, a 4.3x increase. HALF OF IT IS INVISIBLE FROM `/proc`: only the task-side canvas appears in the heap range: the kernel-side `content` surface is `kmalloc`'d and shows up nowhere a process can see, which is its own small observability debt. The lazy version maps pages as the window grows — the mapped region is always a PREFIX of the reservation, so growth stays append-only and still needs no shootdown — but mapping into a foreign address space from the compositor's thread needs a task reference or a deferred-work channel, since the owner can exit between the decision and the mapping. Booked BEFORE coding per the known-debt rule; take it when window counts or window sizes make the standing cost real | Perf | M | when a desktop holds enough windows for 6MB apiece to matter | `window.h` canvas_cap_w comment / `gui_client.c` pivot |
| ~~**A resized gterm lies about its size**~~ **PAID 2026-08-25 — the SIGWINCH slice (PTY.md § Resize): syscall 51 `pty_resize`, signal 28 admitted with `pty_resize` as its producer, gterm's arm calls it, `/tests/winchtest` in the suite with its broken-kernel control run first. The build found what the design missed: every park loop ended only for terminating signals, so a caught WINCH would have arrived at the next keystroke — `signal_park_must_end` now asks "terminate, or anything a handler will catch" at all twelve sites. Original row kept as the record:** — the window resizes, the pty grid does not: a grown terminal letterboxes, a shrunk one clips its right and bottom edges away. The other half is grid realloc plus a notification to the program inside, which is SIGWINCH's entire job description and the reason `stty size` exists. PTY.md booked it before the terminal existed ("Resize — grid realloc + child notification; gate: window resize"); that gate is now open. **THIS ROW USED TO SAY THE NOTIFICATION "WANTS THE CLIENT-NOTIFICATION SEAM". IT DOES NOT, and the sentence was written 2026-08-19, before the event queue carried `WINDOW_RESIZE` and before ring 3 could catch a signal at all** (2026-08-25 correction). It is TWO hops, and only one of them is a notification problem: **WM → gterm already arrives** on gterm's own event queue and is ignored at the resize arm, no new mechanism needed; **gterm → the program inside the pty is SIGWINCH**, whose number 28 is already reserved. The seam is about when a GUI client WAKES and paints (publish-ack, theme reload, retiring the snapshot poll); a signal is about a fact a process must hear, and a program inside a pty has no window and no queue — which is exactly why 4.3BSD invented SIGWINCH. **The two are independent: neither blocks the other, and the seam changes when the event loop wakes, not what it reads, so nothing gets written twice.** Third piece, separable and smallest: a way for a program to ASK its terminal's size (`ls` row item b, our `stty size`), because a signal that says "it changed" is useless to a program with no way to read what it changed TO | Feature-gate | M | now — its gates are open and it needs nothing unbuilt | PTY.md Resize row / `gterm.c` resize arm |
| **`theme.conf` is read from a hardcoded `/home/theme.conf`, not the config ladder.** Every other reader (logd, husk, os64get, hosts, net.conf, desktop, gui.conf, gclock, menu.conf) goes through `os64_conf_find`; libui's theme loader predates the ladder and still spells `THEME_CONF_PATH` itself. Consequence: there is no `/etc/theme.conf` for the system to ship a default look in, and `/sys/conf` cannot say which theme file was read. Noticed while giving the root menu its colours (2026-08-25) — the menu family's keys went into the same table, so the fix is one `os64_conf_find("theme.conf", …)` in `os64_ui_theme_init` plus an `etc/theme.conf` in the image lists | Cleanup | S | next time anyone touches ui.c's theme loader | `libos64/ui.c` THEME_CONF_PATH / docs/conf_path.md |
| **The environment lives in husk.rc, and programs no shell started never see it.** Found the morning the root menu could seat `top` in a gterm with no husk in the chain (2026-08-26): environment inherits from the SPAWNER, so a menu-launched program gets the desktop's, which got the kernel's seed (`PATH=/bin`) — and the day husk.rc says `export EDITOR=/bin/scribe`, everything launched from a menu, a dock, or `gterm <program>` is a stranger to it. Costs nothing today; it is the seam that has not been chosen yet. Unix chose it twice, badly — `.profile` (per-shell, misses GUI launches) versus `/etc/environment`/PAM (system-wide, runs no script) — then every desktop grew a third layer (`~/.xsessionrc`, `~/.pam_environment`, `environment.d`) because shell config had been allowed to become where the environment lived. **The os64 shape, proposed and not yet ruled:** the environment is the SYSTEM's and the shell is one consumer of it — an `env.conf` on the config ladder (`/home` over `/etc`, `key = value`), read once by whoever is init and seeded into every task before anything is spawned; the kernel's `PATH=/bin` seed becomes that file's default instead of a constant. husk.rc keeps what is genuinely a shell SESSION's (aliases, the prompt, `cd`). Then a program launched from the menu, from a gterm, from VT2's husk or at a text boot on the P5 sees one world. Shape it with Chris before coding — it touches init (kernel today, husk-as-init tomorrow) and the env seed in task_create | Design | M | before husk.rc grows its first `export` that a GUI program needs | `task.c` env seed / `husk.rc` / docs/conf_path.md |
| **grootmenu has no keyboard navigation.** Arrows/Enter/Left/Right through the levels is what every menu since Windows 3.0 does; v1 answers only Escape. The arrow keys have no ASCII and arrive in two scancode dialects (PS/2 vs HID — the F-key rule in os64/gui.h), so the right shape is a libui helper (`os64_ui_key_is_arrow`, sibling of `os64_ui_key_is_esc`) that both gterm's burst decoder and the menu would use, not a scancode table in the menu | Feature | S | when a keyboard-only session meets the root menu | `grootmenu.c` handle() / `ui_text.c` VT100 burst decoder |
| Mouse wheel + 5-button (IntelliMouse sample-rate handshake) | Feature-gate | S | UX | GRAPHICS #6 |
| ~~**`gui.conf` has TWO readers taking different keys.**~~ **CLOSED THE SAME DAY IT WAS BOOKED (2026-08-25), and not by refactoring: Chris retired the `hello` window outright — "lets remove it. If I want to reminisce, I can run an old build." That key was the only reason the kernel still read gui.conf, so the file now has exactly one reader (`/bin/desktop`) and `kernel/src/gui/startup.c` is deleted entirely. Recorded because the row was right about the smell and wrong about the cure: I offered two refactors and the answer was to delete the feature that caused it.** | Cleanup | — | CLOSED | `userland/apps/desktop` |
| **`os64_draw_blit` is an OPAQUE COPY — no source-over blending.** Every image os64 draws today is a background or a picture, and both want every pixel, so the source's alpha byte is deliberately not consulted (said so at the declaration). The first real customer for blending is LAUNCHER ICONS drawn over a wallpaper, and it arrives with a second half: libimage refuses BI_BITFIELDS BMPs, which is how a BMP actually carries alpha (BI_RGB's fourth byte is reserved and is zero in real files — trusting it would decode every 32-bit BMP to a fully transparent image, and the host test pins that). Take BOTH together or the blend has nothing transparent to blend. Booked before coding, per the known-debt rule: adding a blend nothing asked for and nothing had tested is how a primitive acquires a mode that is wrong and unnoticed | Feature-gate | S | with the launcher's icons | `os64_draw_blit` / `image.c` BMP arm |
| ~~**USB HID mouse**~~ **PAID 2026-08-17 — by CHRIS** (e497d91: controller fleet + per-device xhci_hid_t + boot-mouse decoder into input_inject_mouse; combo-receiver rebind bug caught in review, fixed same hour). RESIDUE, the real row now: **some P5 port combinations enumerate HID dongles ("attached") but their interrupt endpoints never deliver** — most rear-rear placements were deaf at any distance; USBQUIET neither causes nor cures it. The P5 is FIVE Rembrandt xHCI controllers (64:00.3/.4 = 1022:161d/161e USB3.1; 65:00.0 = 161f USB2-only; 65:00.3/.4 = 15d6/15d7 USB4-side), so suspicion ranks: the USB4-side controllers' interrupt endpoints, per-controller interval/speed quirks, cross-controller connector twins. KNOWN-GOOD CONFIG (do not disturb casually): mouse = front right (facing the P5), keyboard = top rear slot. Fix rides the Supported Protocol capability walk + link power management slice — which is also what makes USBQUIET safe enough to default | Robustness | M | with the xHCI LPM/protocol-walk slice (also unlocks default 2.4GHz hygiene) | Chris's port-fiddling campaign 2026-08-17; kUSBQuiet's scar comment |
| **xHCI is polled, and the poll rides dispatch frequency** — `xhci_poll` runs from `processSignals`, so USB input liveness is exactly scheduler-pass liveness. On a mostly-idle tickless GUI boot with no logd heartbeat, passes arrive SECONDS apart and queued HID reports trickle in one per kernel-task wake (measured 2026-08-21 in QEMU: reports metronome-spaced at exactly 1000 ticks — the status-print cadence — while the compositor's HALT loop, which never crosses processSignals, composited obliviously at 50fps). The P5 never feels it because logd's heartbeat keeps dispatches frequent, and keyboard+mouse sharing one FIFO event ring means a click can never outrun the chord modifiers ahead of it — but that comfort is one daemon deep. Fix is xHCI interrupt wiring (MSI-X or INTx, the e1000 precedent), which the driver's header has anticipated from day one; a cheaper stopgap is a compositor-loop nudge. Surfaced by the Ctrl+Alt chord hunt, where QEMU's PS/2 click kept lapping the starved USB ring | Robustness | M | before any USB input runs on a boot with no other heartbeat (or with the LPM/protocol-walk slice, its natural neighbor) | xhci.h "minus the interrupt" comment; the 2026-08-21 chord-publish hunt |
| Alpha translucency (X byte in XRGB reserved; `surface_blit_masked` already shapes) | Feature-gate | M | UX | GRAPHICS #7 |
| Runtime resolution switching (only `framebuffers[0]` used) | Feature-gate | M | UX | GRAPHICS #8 |
| Verify SIGSLEEP wake latency holds under load (fixed to ~1 tick; unverified at load) | Cleanup | S | none | GRAPHICS #10 |
| **libdraw/libui** userland graphics library — port surface.c primitives to ring-3 (L1) + the widget model (L2, grown app-driven); embedded PSF1 font; cadence-agnostic frame loop | Feature-gate | M–L | after GUI syscalls 16-22 land in the dispatch table + libos64 scaffolding | LIBDRAW.md |
| **The terminal app needs a pty-shaped seam** — a userland program owning another program's stdio (husk's stdin/stdout wired to a GUI window instead of a VT). No such kernel object exists: tty binding is task↔VT only. Unix hit this wall in the '70s and invented the pty (a pipe that lies about being a terminal — why `script`, Emacs shell-mode, and every terminal emulator since work); os64's shape will be its own, likely a handle pair with one tty-shaped end. Same machinery the pipes-into-pagers `/dev/tty` problem wants (Chris's "not now", 2026-08-13) — one design serves both. Booked at GUI-toolkit arc start, BEFORE coding, per the known-debt rule | Feature-gate | M | GUI toolkit Phase D (design) / Phase E (the terminal is the consumer) | GRAPHICS.md phases 2026-08-17; tickless-backstop candy list |

## Build system

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **SUPERSEDED 2026-08-22 — do not build this.** The row used to say the "real fix" for GDB's address ambiguity was PIE with a loader-assigned per-TASK load bias. It is not a fix and cannot be one: **shared text requires ONE virtual address per image across every task that maps it**, which is exactly what makes a page shareable, so the moment libos64 became a `.so` a per-task bias became incompatible with the feature it would have been serving. `upper \| upper` will always share a base. Two of the three stated limits were paid a different way the same day: the 16MB image cap and the 104-slot count were both artifacts of the slot size, and shared libraries shrank app images by 90% (13MB of `bin/` → 1.8MB), so the slot went 16MB → 4MB and the count 104 → **443**. The hard ceiling underneath is `-mcmodel=small` (non-PIE apps must live in the low 2GB — ~128 apps at 16MB, ~500 at 4MB) and Chris named it from memory before anyone counted. The third limit — two instances of one program being indistinguishable — is now permanent, and its real successor is the row below | Superseded | — | never | `app_bases.py` header comment |
| **An in-OS debug server (GDB remote protocol), and the retirement of the whole unique-address scheme.** THE LINUX ANSWER, transplanted: Linux never makes addresses unique — every non-PIE binary loads at `0x400000` and PIE gets a *random* bias — because its debugger is PROCESS-AWARE (ptrace attaches to one process; gdb "inferiors" each carry their own symbol table and address space; `/proc/<pid>/maps` says where things landed; the linker's `r_debug` rendezvous tells gdb when a `.so` appears and at what bias). os64 pays a **no-MMU tax on a machine that has an MMU** — unique link addresses are a technique from DOS TSRs and ROM overlays, needed here only because host GDB stares at QEMU's flat linear space with no idea what a task is. A gdbserver speaking the remote protocol out of the running OS ends the entire problem class: `app_bases.py` retires, the low-2GB ceiling stops mattering, and `upper \| upper` becomes two inferiors. **T-SHIRT SIZE: L**, and it decomposes — (1) the wire protocol and a transport, S–M, the protocol is simple text packets and os64 has both serial and TCP; (2) stop/continue/step of a chosen task plus register read/write, M, needs the scheduler to hold a task stopped and hand over `thread->regs` (STOPPED state already exists); (3) memory read/write in another task's address space, S, `paging_walk_paging_table` + HHDM is already the house technique; (4) breakpoints, M, int3 patching means writing to shared, CoW, possibly read-only text pages — the one genuinely new mechanism, and it interacts directly with the page cache built today (a breakpoint must privatize, or every process running that binary gets it); (5) the symbol/bias handshake, S, since `/proc/<pid>/maps` already names every region including `/lib/libos64.so` and its base. Item (4) is where the surprises live | Feature | L | when debugging two instances of one program stops being a curiosity and starts being a Tuesday | `app_bases.py` / SUCCESSION § harness / PROC.md |
| `--defsym` **must precede `-T`** on the ld command line or it is silently ignored and every app links at the `0x400000` fallback — builds fine, runs fine, and fails only in the debugger. Guarded by a comment at the flag; a build-time assert (entry == assigned base) would make it loud instead of documented | Robustness | XS | none | `userland/GNUmakefile` APP_RULE |
| **THE NX ARC NEVER REACHED THE `kernel/test/elf` FIXTURES — the ones with a `.bss` run RWX.** That script places `.text`/`.rodata`/`.data`/`.bss` with no page alignment between them, so ld folds them into ONE LOAD segment carrying the union of their permissions: `readelf -lW kernel/bin/stat_test` answers `RWE`, where an app answers three segments (`R E`, `R`, `RW`). The apps were fixed on 2026-08-16 and app.ld's "PAGE-ALIGN EACH PERMISSION CHANGE" comment carries the whole argument — including the part that matters here, that **the kernel derives page protections from segment flags**. So these fixtures genuinely run with writable text and executable data. Found 2026-09-01 when `stat_test`'s buffer grew and the fixture re-linked, surfacing a warning ld had been printing all along — a fixture only re-links when its OWN source changes, which is why it went unseen for a fortnight; confirmed pre-existing by rebuilding the same fixture at its old buffer size (identical `RWE`). A fixture with no `.bss` escapes by accident, not by design. Severity is low because fixtures are trusted code that ships to no one, but the proof harness runs under weaker memory protection than the system it is proving. NOT the `userland/tests` fixtures — those link as apps and are already split (`nx_test`, of all things, is one of them). Fix = the `ALIGN(CONSTANT(MAXPAGESIZE))` boundaries app.ld already has | Robustness | S | none | `kernel/test/elf/linker.ld` vs `userland/link/app.ld` |
| os64 FAT driver can't read a sub-FAT32-minimum volume: `mformat -F` forces FAT32, but <65525 clusters (~34MB) yields a malformed hybrid the driver misclassifies as FAT16 → every open fails. Currently worked around by the 64MB DISK_SIZE_MB floor. Fix = teach the driver FAT16 (or drop `-F` and let mformat pick), so the ramdisk can shrink below 64MB | Robustness | M | if the ramdisk ever needs to be <64MB | GNUmakefile disk target / fat driver |
| ~~**`os64 refresh` no longer carries the fixtures**~~ **PAID 2026-08-29, same day.** Booked when `/tests` split out of `/bin`: the valet routed by FILE NAME, the fixtures share no suffix to key on, and `os64serve.py` skips subdirectories — so an over-the-wire refresh kept `/bin` current and left `/tests` at whatever the last ISO burn delivered. The fix is LOTS: a served directory may carry a label (`os64serve.py .../userland/bin/tests=tests`), every LIST line gained a fourth field naming it, and `os64get.conf` grew an `@lot` rule class. The lot names the SOURCE, never the destination, and ranks BELOW exact and suffix rules — which is what lets one served directory hold files bound for several places (`kernel/bin` carries fifteen fixtures for `/tests`, plus `os64_kernel` for `/fat/boot` and `libtest.so` for `/lib`). The field is APPENDED so an older os64get reads the first three and stops; all it loses is the catalogue CRC (its hex parser wanted exactly eight characters to end of line), so that one transitional refresh re-checks each file against its own per-file header — slower, never wrong, and healed by the newer os64get it just fetched. A single-file fetch asks for a LIST it would not otherwise need, but ONLY when the conf has `@lot` rules and no DEST was given. Verified over slirp: `-a` reported 103 files 102 unchanged (a wrong route would have installed 47 fixtures into /bin instead), and a bare `os64get HOST fputest` answered `/tests/fputest` | Feature-gate (PAID) | — | done | `os64get.conf` / `tools/os64serve.py` / `os64get` |

## Shared libraries (booked 2026-08-22, the day libos64 became a `.so`)

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **A refresh that replaces open files leaves orphaned inodes on disk until the next boot — expected, and now permanent for libos64.so.** Confirmed 2026-08-22 while testing `os64get -a`: after a run that replaced 83 files, `e2fsck -fn` on the image reports "Inodes that were part of a corrupted orphan linked list" plus bitmap differences, and ONE more boot clears it completely (122 → 114 inodes, spotless). That is the ext2 orphan chain doing exactly its job — a file unlinked while open has its name removed at once and its storage freed at LAST CLOSE, with the on-disk chain as the crash record. What shared libraries changed is the duration: husk and logd release their old inode when they exit, but **the loader holds `/lib/libos64.so` open forever (no unload)**, so a replaced library's old inode stays orphaned until the machine reboots. Nothing is lost and nothing is corrupt; the cost is that fsck cannot be green between a refresh and a reboot. Two things follow — don't read a post-refresh fsck as damage, and note that `make fsck-ext2` normally runs on a freshly BUILT image and so never sees this at all. **PAID 2026-08-28 by unload-at-refcount-zero**, exactly as this row's gate said it would be: an object is unloaded the moment nothing holds it, which closes the handle, which is what makes ext2 reap the orphan. Verified end to end — a session that replaced two RUNNING binaries by rename, then let the old copies exit, left `make fsck-ext2` green on both images with no orphan complaint. The duration is now the honest one: a replaced binary's old inode lives exactly as long as the programs still running it | Cleanup (PAID) | — | done | ext2 orphan chain / `shared_object_unload_locked` |
| **A replaced `.so` on disk does not take effect until reboot, and the old one keeps working even after it is deleted.** The registry is keyed by PATH STRING and never unloads, so once `/lib/libos64.so` is resident, `shared_object_load_or_get("/lib/libos64.so")` is a cache hit that never touches the directory again. Demonstrated live: renaming the library out from under a running system, then starting programs that had never run before — they all started fine. Half of that is *correct and Unix-like* (a mapped image outliving its directory entry is exactly what an inode reference buys, and ext2's orphan chain already makes it true for the file). The half that is a real gap is the other direction: `os64 refresh` replacing libos64.so leaves every subsequent spawn on the OLD code with nothing saying so. The fix is retention policy — notice the inode changed, or unload at refcount 0 — and it is the same slice already deferred for unload, which is the same slice deferred for the block read cache. **PAID 2026-08-28, and it needed BOTH halves of that sentence, not either.** Unload-at-zero alone would have missed precisely the files worth refreshing (husk, logd and libos64.so are never at zero — something is always running them), and the identity check alone would have left every idle program resident forever. So: every load opens the path and compares the file's identity (`vfs.h` f_ident — an ext2 inode number), a changed identity RETIRES the resident copy (transitively, so its dependents rebuild against the new library too) while its current holders keep running it, and an object with no holders is unloaded. The old direction is unchanged and still correct: a program that is running keeps the image it started with | Cleanup (PAID) | — | done | `shared_object.c` retire/reload; `test_shared_object_reload` |
| **Symbol lookup is a LINEAR SCAN of the whole dynamic symbol table, per relocation.** `shared_object_find_symbol` walks every `.dynsym` entry doing `strcmp`. libos64 exports 158 symbols and a page carries a handful of relocations, so this is free today; it is O(exports × relocations) and would stop being free at a few thousand exports. The irony is that `DT_HASH` is ALREADY PARSED — the loader reads its `nchain` word to learn the symbol count, and then ignores the hash buckets sitting right next to it. Using them properly is maybe thirty lines | Cleanup | S | when a library exports thousands of symbols, or first-touch latency shows up in a profile | `shared_object.c` `shared_object_find_symbol` |
| **A same-page fault collision still SPINS.** A core that faults a page another core is mid-resolution on busy-waits (with interrupts deliberately re-enabled, see the comment) for the duration of a disk read. Correct, and the wrong shape: the faulting thread should USLEEP on the I/O completion and be woken on publish. Carried over from the original design, unchanged, and now reachable far more often since every program's own image goes through this path | Cleanup | M | when a fleet of tasks starting at once makes it measurable | `shared_object.c` `shared_object_resolve_page` |
| **PAID 2026-08-22 (and the first fix was wrong, which is the instructive part).** `make -C userland` built with the HOST toolchain while `make` from the repo root built with the cross one — same tree, same makefile, different compiler depending on which directory you typed in. `userland/GNUmakefile` says `CC ?= x86_64-elf-gcc` / `LD ?= x86_64-elf-ld`, and that is a NO-OP: `CC`/`LD` are make BUILT-IN variables (`cc`, `ld`), and `?=` only fills in an *undefined* variable, not one with a built-in default. From the root the value is inherited from the parent make and the cross tools win, which is why nothing has ever visibly broken; build inside `userland/` and you silently get host gcc and host ld. Cause: `CC ?= x86_64-elf-gcc` is a no-op when `CC` is defined, and `CC`/`LD` are make BUILT-IN variables (`cc`, `ld`). What saved the root build all along is `MAKEFLAGS += -rR` on line 2 of the root GNUmakefile — `-R` is `--no-builtin-variables`, which propagates into the sub-make and leaves CC/LD genuinely UNDEFINED, so `?=` fired there. Build straight from `userland/`, with no `-rR` in play, and the built-ins are back and `?=` goes quiet. **The obvious fix — `ifeq ($(origin CC),default)` — is exactly wrong**: under `-rR` the origin is `undefined`, not `default`, so it fixed the direct build and BROKE the root build (every link ran with an empty `$(LD)`, and make strips the resulting leading `--` as its own ignore-errors prefix, so the error read `defsym: No such file or directory (ignored)` — a genuinely baffling message for a missing linker). Shipped fix assigns when the origin is `default` OR `undefined`, and never when a human meant something (command line, environment, parent make). All four paths verified. It mattered because the two toolchains have DIFFERENT DEFAULTS — host ld emits `--hash-style=gnu`, bare-metal binutils sysv, and the kernel's loader needs sysv (it takes the dynamic symbol count from DT_HASH's nchain); that one is now pinned explicitly on both link lines | Cleanup (FIXED) | — | done | `userland/GNUmakefile` CC/LD |
| **Every dynamically-linked EXECUTABLE ever run is retained at refcount 0, forever.** The warm-cache retention policy was written for one library and silently widened to every app the day userland went dynamic: `cat /sys/shlib` after boot shows `/tests/hog` at `refs: 0` with its page frames, `page_phys[]`, parsed tables, and an OPEN ext2 handle — and each of the ~64 apps joins it on first run. Two consequences beyond memory: `shared_object_find` is a linear `strcmp` walk that grows by one per distinct program ever run, and every replaced binary's old inode rides the orphan chain until reboot (the row two above, multiplied by the whole of /bin). Found by the 2026-08-23 review of the shlib commit. The fix IS the unload-at-zero slice already booked — this row exists so that slice knows its real population is executables, not libraries. **PAID 2026-08-28.** `/sys/shlib` on a running system now lists only what is in use — every entry at `refs: 1` or more — and a fourth consequence turned up while paying it that nobody had booked: ext2's open-inode table is 64 slots, so a boot that ran 64 distinct programs would have filled it and started refusing every open of a file it did not already hold | Cleanup (PAID) | — | done | `shared_object.c` retention / `/sys/shlib` |
| **`app.ld` and `lib.ld` carry byte-identical section blocks** — the four PLT sections, the eight read-only dynamic-metadata sections (`.hash` … `.eh_frame`), and the `.dynamic`/`.got`/`.got.plt` trio, with the same "canonical names or they evaporate" commentary twice. ld's `INCLUDE` would make those blocks one shared linker-script fragment. The second library paid the two adjacent debts from the original row: `--lib-slot-size` now feeds `lib.ld`'s slot ASSERT, and `app_bases.gdb` uses bare `add-symbol-file` for the prelinked libraries instead of rediscovering their absolute `.text` addresses by regex. What remains is extracting that shared fragment, whose value arrives when either script next needs the duplicated block changed | Cleanup | XS | the next dynamic section either script needs | `userland/link/*.ld` |
| **A spawn re-reads and re-parses the image of any program with no live instance** (booked 2026-08-28, the day unload-at-zero landed — the price of the retention policy, stated rather than discovered later). Every `shared_object_load_or_get` opens the path (it must, to compare identity), and a miss then runs `elf_parse_image` — section headers, dynamic tables, and `.symtab`/`.strtab` when stack traces are on — plus one disk read and one relocation pass per page the program touches. Everything after the first time comes out of the block cache, so it is CPU and kmalloc churn rather than I/O, and the object that would actually hurt to reload (libos64.so) never unloads because husk pins it. NOT MEASURED: the honest statement is that no slowdown was visible driving the shell by hand, and nobody has A/B'd it against a build that retains | Cleanup | S | if a spawn-heavy workload (`watch -n 1 "ps -ef"` is the house's worst case) ever feels slower — measure before optimizing, and the answer is probably a parse cache keyed by identity, not a return to retention | `shared_object_load_or_get_locked` |
| **Objects in a DT_NEEDED cycle can never be unloaded**, because their references are each other's. A depends on B and B on A: each holds an edge on the other, so neither ever reaches zero even after every task using them is buried, and both stay resident (and registered, and holding their files open) until reboot. This is refcounting's oldest known limitation and the cure is a mark-and-sweep over the registry, which is a great deal of machinery for a condition os64 has not produced: its two libraries form one acyclic edge (`libgzip.so` needs `libos64.so`; `libos64.so` needs no library), and the loader's cycle handling exists because DT_NEEDED can express a cycle | Cleanup | M | the day a real dependency cycle exists — which wants a reason first, since a cycle between two libraries is usually a design mistake | `shared_object.c` deps[] |
| **An in-place rewrite is invisible to the identity check.** Identity is the ext2 inode number, so replacing a file by RENAME is noticed (the new file has a new inode) and truncating one in place is not. That is sound today by construction rather than by luck: while an object is loaded its file is open, and ext2 REFUSES to truncate an open file — so `cp`'s "w" mode cannot rewrite it behind the registry's back. The gap is the `"u"` mode (open for update without truncation), which is not refused and which no shipped program uses. A future byte-patching editor pointed at a running binary would have its edits ignored by every later spawn until the last holder exits. Cheap cure if it ever matters: fold size and mtime into the recorded identity | Robustness | S | when something in userland opens a file `"u"` | `shared_object_t.ident` / `ext2_truncate_locked` |
| **The shared-library window's bump allocator is reclaimed only when the unloading object was the last one placed.** An exact reclaim in the common case (load one PIE image, unload it, load it again) and an abandoned span otherwise, exactly like the failed-load unwind it copies. Unloading turns bump slots into something a running system consumes repeatedly rather than once per distinct image, so the march is now unbounded in principle — at 512GB of window against images measured in kilobytes it needs millions of interleaved loads to matter, and only images that arrive WITHOUT an address of their own are in the bump region at all (a prelinked library and a non-PIE executable both take load_bias 0 and consume nothing) | Cleanup | S | if the window ever runs out — which prints its own diagnosis | `shared_object_unload_locked` / `kSharedObjectNextVirt` |
| **The relocation set is three types wide** (`RELATIVE`, `GLOB_DAT`/`JUMP_SLOT`, `64`). No TLS, no IFUNC, no `COPY` — and `COPY`'s absence is a RULING, not a gap (libos64 exports functions, not variables; see DIVERGENCES). No `.init_array`/`.fini_array` execution either, which is the one that will bite first: LIBOS64.md promises ELF constructors and destructors for at-exit handle cleanup, and the loader does not run them | Feature-gate | M | when libos64 wants a constructor, or C++ ever happens | `elf_relocate.c` / LIBOS64 § startup |

## Networking (the arc's booked honesty — NETWORK.md carries the arguments)

| Debt | Sev | Cost | Gate | Source |
|---|---|---|---|---|
| **The P5's RX path went silent mid-transfer (2026-08-22)** — seen ONCE: `os64get` fetching its own replacement over the RTL8125 froze partway, the hub's lights kept blinking (the valet was still sending), a ping to the P5 got no answer, the log said nothing, and a reboot made the same transfer complete first try. The fingerprint says RECEIVE, not wire: frames arriving, nothing consumed — a stalled RX ring (descriptor ownership never handed back, or an interrupt the driver stopped hearing), not a dropped link. Nothing lost thanks to the `.part` discipline. UNDIAGNOSED; the next occurrence wants `/sys/bus/pci` and the driver's ring counters read BEFORE the reboot — an observability row as much as a bug row. Candidates worth a look when it recurs: the r8125 ring's refill under back-to-back full-MTU bursts, and the processSignals poll rider's relationship to a lost INTx (see the rows below) | Robustness | ? | the second time it happens — with the counters in hand | r8125.c / r8125_ring.c / os64get |
| ~~**`net_tcp_refused` fails "randomly" on the P5**~~ **PAID 2026-08-27, same day** (one post-boot failure in six boots, Chris): the dial that TIMES OUT with no RST counted is now a SKIP that names the reason — the gateway answered port 9 with silence, which is a fact about the router (a firewall, or a home router rate-limiting its own RSTs), not the stack. A dial that DID get an answer is still held to all three assertions. Whether the P5's gateway is silent or slow is still unread; if the SKIP shows up there often, the line says so | Flake | S | kernel/test/test_main.c test_net_tcp_refused |
| **`reset` means two things, and the morgue's `rst` flag can only say one.** `tcp_conn_t.reset` is set by an arriving RST *and* by tcp_poll when the retransmit retries exhaust — so a `/sys/net/tcp` row whose peer merely went silent reads `rst`, the word the eyes slice reserved for "the machine answered and said no". Harmless while nothing detached could exhaust (a bare FIN was never retransmitted before 2026-09-02, so a detached close never reached the exhaustion branch); now a close into a dead link ends there ~31s later and wears the wrong flag. Shape when paid: a second bit (`abandoned`) set by exhaustion, `reset` reserved for a RST that arrived, the row and dial's `why` reading both. Booked rather than done because every `c->reset \|\|` wake and return test would grow a sibling, which is the kind of sweep that earns its own review | Correctness | S | the first time someone reads `rst` on a row and goes looking for a peer that refused | tcp.c `tcp_poll` exhaustion branch; sysfs.c `r->rst = c->reset` |
| **DHCP lease renewal** (T1/T2 timers) + RELEASE/DECLINE courtesies — v1 leases once and never renews; slirp/VBox leases are functionally eternal, a real LAN's are not | Feature-gate | M | before long uptimes on a real LAN (the P5) | dhcp.h policy note |
| IPv4 fragment REASSEMBLY — arrivals with MF/offset drop loudly (counted + logged). Modern DF-world paths rarely fragment; "rarely" is not "never" | Feature-gate | M | when something real fragments at us | ipv4.c fragment stance |
| ICMP port-unreachable when UDP has no binding (how `traceroute` ends and `nc -u` probes learn) — currently counted-and-silent | Feature-gate | S | when a consumer needs the courtesy | udp.c rx_no_binding |
| **virtio-net polls** (processSignals rider, the xhci_poll precedent) — NETWORK.md Phase 1 planned MSI-X from the start; polling shipped first because it made packets move the same day. The e1000 crossed over 2026-08-06 (INTx doorbell, probe-confirmed routing); virtio stays polled ON PURPOSE until its own conversion — virtio interrupts are per-queue MSI-X machinery, a different slice. Mind the house rule (AP-routed vectors ≥ 0x40) | Feature-gate | M | before any throughput-sensitive consumer (TCP at real speeds) | virtio_net.c / NETWORK.md Phase 1 |
| processSignals runs the device polls AND the whole inline protocol stack UNDER kSchedulerSwitchTasksLock — its hold time scales with traffic. Chris's ruling (2026-08-01): fine under BSPSCHED at current load (the beefy pass runs on the core that owns scheduling anyway); do NOT shrink the lock scope now — that cleanup lands WITH the net bottom-half thread (the MSI-X row above), when the polls move to a kworker and processSignals returns to sleepers + wake sweeps. Measure (DEBUG_NET cycle stamp) before any surgery | Cleanup | S | with the MSI-X / bottom-half work, pre-TCP-throughput | signals.c processSignals / 8/1 design conversation |
| **e1000 INTx Stage 2 — the bottom half**: today the ISR raises kE1000RxWork and processSignals drains on its next pass (doorbell ends the empty-mailbox reads; latency still ≤ one pass). The full prize is the ISR WAKING a drainer thread directly — which needs a wake-from-ISR primitive the scheduler doesn't have (wake sweeps live in processSignals precisely because of the 9badced lock saga). DESIGN CONVERSATION BEFORE CODE, per the known-debt-beforehand rule (2026-08-06) | Feature-gate | M | with the bottom-half/lock-scope work above — one design, three rows retire together | e1000.c e1000_isr / 8/6 briefing |
| **No shared-INTx framework**: e1000's ISR practices shared-line etiquette (ICR==0 → count a stranger, decline politely) but nothing CHAINS handlers on one GSI. Also note: e1000_enable_intx sets PCI Interrupt Disable (command bit 10) on every OTHER device — os64's drivers all poll, so this makes the de facto official; the day a second driver wants a doorbell it must clear its own bit AND this framework gets built. intx_strangers climbing = a neighbor moved onto GSI without telling us | Feature-gate | S | the day a second PCI device adopts INTx | e1000.c enable_intx probe-safety comment |
| **Loadable driver modules** (booked 2026-08-21, Chris's pick; ratified as a FEATURE, not a sneakernet cure — HD boot solves the logistics alone, since the kernel is one file). v1 shape agreed in the sizing talk: LOAD-ONLY at boot (unload is where it becomes XL — quiescing IRQs, refcounts on every registration, in-flight I/O), resolve against the kernel's own symtab (symbols.c already carries 1554 names — the export table exists), relocate with the machinery elf_relocate.c already proves every boot, load into a module VA arena within ±2GB of kernel text (mcmodel=kernel reach — the same constraint that gave Linux its module area). The chicken-and-egg blesses the split: whatever mounts root (NVMe/AHCI, ext2, console) stays built-in; NICs, USB, GUI are the candidates — or modules ride Limine module_path lines and load pre-root. Prerequisite that pays double: a poll/IRQ registration seam — processSignals calls xhci_poll/virtio_net_poll/e1000 BY NAME today, and the "generic poll verb" tidy-up two rows up waits for a third driver; a module loader IS that third driver. Kernel W^X (the NX arc's booked half) is the natural companion for module text. Lineage: Linux shipped its first four years all-built-in too — insmod arrived with 1.2 in 1995; SunOS 4 beat them to it | Feature | L | after HD boot; wants the poll-registration seam + module VA arena designed first (known-debt-beforehand: design talk before code) | 2026-08-21 sizing conversation; symbols.c / elf_relocate.c as the down payment |

## Explicitly NOT debts (recorded so they aren't re-litigated)

- **`limine.conf` will NOT be delivered by `os64get`, and the boot menu is
  the P5's own.** Chris's ruling, 2026-08-29: *"I don't think I'll ever want
  to automatically update limine.conf on the P5 using os64get. I just didn't
  realize that the new boot command existed."* The routing rule in
  `os64get.conf` stays for a hand-fetch, but nothing will stage the file into
  a served directory to make `-a` take it. The reasoning is the file's:
  it is the one thing on the boot volume that can make the machine
  unbootable, and — alone among the things that travel — it is exercised only
  on the P5 and never in a harness.
  **SO THE COST LANDS ON WHOEVER ADDS A CMDLINE TOKEN, and it is a HUMAN
  cost, not a technical one:** a new flag works in QEMU and is silently
  absent on the P5 forever, because his boot entries are edited by hand.
  Adding a token to `limine.conf`/`limine-hd.conf` is therefore not
  "shipping" it — SAY SO OUT LOUD in the same breath, so he can add it. That
  is what went wrong with `CRON`, and the fix was him typing four letters
  once he knew it existed.

- **A popup survives a VT switch, and that is the intended behavior.** Raised
  as a P2 in PR #34 (Ctrl+Alt+F1..F7 hands the glass to a text VT without
  moving WM focus, so an open root menu is still there when VT8 comes back)
  and DECLINED 2026-08-26, tested first: switching VTs does not touch the
  GUI, nothing was clicked, and no window took the focus. A popup dismisses
  on FOCUS-LOSS, which is a thing that happens inside the GUI; returning to
  a desktop arranged exactly as you left it is what a VT excursion should
  mean. The proposed cure also has a hazard of its own — a menu that dies
  because you glanced at a console — and it would cost a new kernel→WM
  notification path to get it. The finding argued the behavior "contradicts
  transient popup behavior"; no such rule exists here, and the convention it
  came from belongs to toolkits that grab the pointer.

- **libimage: the BMP `bfSize` field is not validated.** Raised as a P2 in PR
  #30's round 7 (a file whose declared length ends before its raster still
  decodes) and DECLINED 2026-08-25. `bfSize` is the least reliable number in
  the format — real writers put 0 there, or a stale size from an earlier save
  — and the decoders that survive contact with the wild (stb_image, Windows'
  own loader) ignore it, because refusing on it rejects legal-in-practice
  files to protect nothing. The guard that matters is the ACTUAL buffer
  length: `decode_bmp` checks `data_off` against it and the row count
  against it by division before allocating, so no byte past the supplied
  data is ever read. The doctrine "a header that disagrees with the file is
  refused" applies to the fields a decode DEPENDS on; a field the decoder
  never uses cannot disagree with anything. Revisit only if a consumer ever
  needs the declared length for something (multi-image containers, say).

- **husk: an EMPTY expansion leaving an adjacent typed `>` as an operator.**
  Raised as a P1 in PR #28's round 6 (`echo safe $1> output` with `$1` empty
  redirects instead of printing) and DECLINED 2026-08-23 — the first Codex
  finding either model has turned down, so the reasoning is written out.
  **Verified against real shells on the host:** bash redirects that line
  whether `$1` is empty OR set (`safe` / `safe XX` both land in the file),
  and dash agrees, because a shell lexes BEFORE it expands — the `>` is an
  operator token the author typed and no value changes what it is. husk
  matches on the empty case, so nothing is wrong there. Where husk really
  diverges is the other half, in the SAFE direction: with `$1` non-empty the
  token becomes `x>`, and husk's operators are token-exact by design
  (`upper<f` is a program named "upper<f" — its own comment), so it passes
  the token through as data where bash would redirect. husk under-recognizes
  operators rather than over-recognizing them. The proposed cure — tracking
  expansion boundaries even when the value is empty — would make husk SWALLOW
  AN OPERATOR THE AUTHOR TYPED: a new divergence, in the surprising
  direction, to fix a case that already matches POSIX. **The one genuinely
  odd corner, recorded rather than hidden:** `>$1>` with `$1` empty yields
  the adjacent pair `>>` (append) where bash performs two separate redirects.
  That is the token-exact rule meeting adjacency, not data manufacturing
  syntax — the author typed both bytes — and closing it means lexing before
  expanding, i.e. rebuilding husk's front end, which is an arc and not a fix.
  THE DISTINCTION THAT DECIDES THIS CLASS: the four findings in the family
  that WERE real (argument bytes becoming operators, an expanded quote
  closing a typed one, a half-substituted `>>`, a glob result read as an
  operator) all had one signature — *the outside world's bytes acquiring
  meaning nobody gave them*. This one is the mirror image: the author's own
  byte keeping the meaning he gave it.
- **Shared kernel upper half** (every task PML4 → kernel's own tables,
  U/S-protected): a deliberate, sound decision for os64's threat model
  (we run our own binaries). Meltdown/KPTI unshared it for a speculative
  side-channel that is out of scope. See ABI § Memory-protection.
- **The "Time to make the donuts" scheduler message**: load-bearing
  tradition, not debug noise. Never remove/reword. See SCHEDULER § donuts.
- **Pipes carry TWO copies (user→kernel ring→user), not shared memory.** The
  tempting "map one page into both tasks" optimization puts the ring's
  head/tail pointers in memory USERLAND CAN WRITE, so a buggy program could
  corrupt or hang the process on the other end. The kernel must be the arbiter
  of the buffer. (Same call GRAPHICS.md makes: syscalls carry handles, never
  pixels.) A future zero-copy `splice` can be added *behind the handle API*
  without changing one line of any program — that is what handles are for.
- **A bounded pipe is not a limitation — the bound IS the flow control.** A
  writer that fills the 64KB ring BLOCKS, which throttles the producer to
  exactly the consumer's speed and is the only reason a pipeline runs in
  constant memory. Do not "fix" this by growing the buffer on demand; an
  unbounded pipe is a memory leak with a friendly API. See `pipe.h`.
- **spawn does NOT blanket-inherit the parent's handle table** (Unix does). A
  child gets its parent's 0/1/2 plus exactly the handles it was explicitly
  given — stdio flows down (since 2026-08-28: `testrun > log 2>&1` was leaving
  every fixture's output on the console, because `-1` meant "the console"
  rather than "mine"), and nothing else does. Deliberate: a child can never
  accidentally hold some unrelated pipe end open, which is a classic way
  pipelines hang. Do not "fix" this into Unix semantics.
- **Write atomicity is capacity-bounded, not `PIPE_BUF`-bounded.** os64's rule
  is one sentence — *a write of ≤ PIPE_CAPACITY lands whole, or it waits* —
  instead of Unix's magic 4096 constant that everyone has to memorize. Reads
  return SHORT and writes land WHOLE; the asymmetry is intentional (a reader
  that waits to fill its buffer deadlocks interactive pipelines; a writer that
  lands whole keeps records intact). See `pipe.h`.
